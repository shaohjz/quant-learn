# 部署报告 2026-07-31（本机 Linux 长期模拟）

## 变更摘要

- 新增 `scripts/linux_sim_runner.sh`：独立 DB `data/sim_local.db` + 产物 `output/linux_sim/`
- 关键脚本认 `QUANT_DB_PATH` / `QUANT_ARTIFACT_ROOT`（`sim.config_resolver`）
- 本机 crontab 已挂交易日 08:35~16:20 六条；默认不推企微
- 产机 Windows schtasks：**未改**

## 本机验收

| 项 | 结果 |
|----|------|
| `./scripts/linux_sim_runner.sh init` | ✅ #1=10万 #3=5万 |
| `day_close` / `pool` | ✅ 写入 `output/linux_sim/` |
| 未覆盖 `pm/trade_journal/` | ✅ |
| `crontab -l` 含 linux_sim_runner | ✅ |
| `LINUX_SIM_PUSH` 默认 | 0（不推企微） |

## 产机

只需 `git pull`；无需重建 schtasks。
