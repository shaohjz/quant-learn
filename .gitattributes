# 复盘产物在 Windows 产机和开发机之间来回提交。不固定行尾的话，
# 每天会多出一份整文件 diff，把「今天到底改了哪个参数」淹掉。
docs/STRATEGY_SPEC.md      text eol=lf
output/strategy_review/**  text eol=lf
pm/strategy_review/**      text eol=lf
# 配置文件同理，且更关键：它们在 Windows 产机上由脚本回写（CRLF），
# 在开发机上是 LF。不固定会每天产生 2800+1200 行整文件 diff，
# 把「今天到底调了哪个参数」彻底淹没。2026-08-29 补上。
config.yaml                text eol=lf
config_auto.yaml           text eol=lf
# 2026-09-16 TES-23：bat 含多行 if 括号块时，LF-only 行尾会令 cmd.exe 解析错乱
# （rc=3、日志停更）。vqlearn_live_runner.bat 固定 CRLF，防回迁/git 同步再写回 LF。
scripts/vqlearn_live_runner.bat text eol=crlf
