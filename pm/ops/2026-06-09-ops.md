# 运维巡检报告 — 2026-06-09

**巡检时间：** 2026-06-09 19:00 (Asia/Shanghai)  
**执行 Agent：** ops-agent-daily (cron: 7835f916-cb1f-485e-863a-adcd4857653e)  
**报告状态：** ⚠️ 发现问题

---

## 1. 行情源检查  ❌ FAIL

| 项目 | 状态 | 详情 |
|------|------|------|
| AkShare 实时行情 | ❌ 异常 | `Connection aborted, Remote end closed connection without response` |
| 最新数据时间 | ❌ 过期 | `data/cache` 最新数据时间：2026-06-05 09:31（距今 **6332 分钟**，远超 5 分钟阈值） |
| `data/trade_dates.json` | ⚠️ 较旧 | 更新于 2026-06-09 15:30（约 3.5 小时前，今日收盘数据正常） |
| `data/universe_cache.json` | ❌ 过期 | 更新于 2026-05-25（约 15 天前） |
| `data/sector_cache.json` | ❌ 过期 | 更新于 2026-06-01（约 8 天前） |

**结论：** 行情源（AkShare）当前无法连接，缓存数据严重过期。`realtime_price_gateway.py` 使用 AkShare 获取实时行情，但 `akshare` API 连接被远端关闭。

**严重程度：** S1（影响实时信号生成）

---

## 2. 交易 API 检查  ⚠️ DEGRADED

| 项目 | 状态 | 详情 |
|------|------|------|
| `realtime_price_gateway` 导入 | ✅ 正常 | 模块可正常导入 |
| `get_realtime_price()` 调用 | ❌ 失败 | AkShare 连接中断，返回 `None` |
| `qmt_gateway` 导入 | ⚠️ 部分异常 | 无法从 `qmt_gateway` 导入 `QMTGateway`（实际类名为 `QMTGateway` 需确认） |
| `broker/qmt_broker.py` | ✅ 存在 | QMT 模拟交易 broker 实现存在 |

**结论：** 主行情源（AkShare）不可用，QMT 网关可进一步排查。交易 API 本身取决于行情源。

**严重程度：** S1

---

## 3. 数据库检查  ✅ PASS

| 数据库文件 | 可读 | 可写 | 路径 |
|-----------|------|------|------|
| `pm.db` | ✅ | ✅ | 项目根目录 |
| `data/pm.db` | ✅ | ✅ | data/ |
| `data/sim.db` | ✅ | ✅ | data/ |
| `data/alert_fired.db` | ✅ | ✅ | data/ |

**结论：** 所有数据库文件均可正常读写，无损坏。

---

## 4. 磁盘 & 内存检查  ✅ PASS

| 项目 | 使用量 | 阈值 | 状态 |
|------|--------|------|------|
| 磁盘 C: | 69% (102 GB / 149 GB) | < 80% | ✅ 正常 |
| 内存 | 65.5% (10.3 GB / 15.7 GB) | < 85% | ✅ 正常 |
| 空闲磁盘空间 | 46 GB | — | ✅ 充足 |

**结论：** 磁盘和内存均在安全范围内。

---

## 5. Cron 任务检查  ✅ PASS

| 任务 | 调度 | 状态 | 下次执行 |
|------|------|------|---------|
| `ops-agent-daily` | `0 19 * * 1-5` (Asia/Shanghai) | ✅ 启用 | 2026-06-10 19:00（自动 stagger +5min） |

**结论：** 当前仅有 1 个 cron 任务（即本巡检任务），cron 表达式正确，任务正常触发。运行历史为空（首次运行或历史未持久化）。

---

## 6. 发现问题汇总

| # | 问题 | 严重等级 | 状态 |
|---|------|---------|------|
| 1 | AkShare 行情源连接失败，`get_realtime_price()` 返回 `None` | S1 | 待恢复 |
| 2 | `data/cache` 最新数据距今 6332 分钟（2026-06-05），严重过期 | S1 | 待恢复 |
| 3 | `data/universe_cache.json` 过期 15 天 | S2 | 待刷新 |
| 4 | `data/sector_cache.json` 过期 8 天 | S2 | 待刷新 |

---

## 7. 自动恢复尝试

### 尝试 1：重试 AkShare 连接（S1）

```python
# 重试 akshare API（更换接口函数）
import akshare as ak
df = ak.stock_zh_a_spot_em()  # 尝试东方财富接口
```

**结果：** ❌ 同样 Connection aborted — AkShare 远端问题，非本机网络问题。

### 尝试 2：检查是否有备用行情源（Tushare）

检查 `config.yaml` 是否配置了 Tushare token，如有则切换。

**结果：** ⚠️ 待确认 — `config.yaml` 中未见 Tushare token 配置。

---

## 8. 建议行动

1. **S1 — AkShare 不可用在 T+1 内恢复**：
   - 检查 AkShare 版本：`pip show akshare`，考虑升级
   - 切换至 Tushare 作为备用行情源（需申请 token）
   - 或接入 QMT 实时行情（已内置 `qmt_gateway.py`）

2. **S2 — 刷新缓存数据**：
   - 运行 `python data/fetch_data.py` 刷新 `data/cache/`
   - 刷新 `universe_cache.json` 和 `sector_cache.json`

3. **监控告警**：
   - 建议在 `notifier` 中增加行情源健康检查失败时的企微告警

---

**巡检完成时间：** 2026-06-09 19:05  
**执行结果：** OPS_DONE（有异常，详见上文）
