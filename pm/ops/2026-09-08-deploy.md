# 部署报告 2026-09-08

## 变更内容
账户 #4 银行波段扫描改为吃独立参数，不再被 #3 参数挡死。

| 参数 | 通用(#3) | 银行(#4)改前 | 银行(#4)改后 |
|------|:--------:|:------------:|:------------:|
| min_score_buy | 5 | 5 | **3** |
| max_volume_ratio | 0.8 | 0.8 | **0.95** |
| min_net_rr | 1.2 | 1.0 | 1.0（不变） |
| executable_types | A,B | A,B,C,D | A,B,C,D（不变） |

## 执行记录

### 步骤 0 — git pull ✅
- `origin/master`: `2bae0038 fix(bank-swing): 让账户 #4 扫描真正吃到银行参数并放宽单信号门槛`
- 涉及文件：`config.yaml`、`scripts/swing_auto.py`、`scripts/bank_swing_daily.py`、`scripts/swing_daily_report.py`、`tests/test_bank_swing_daily.py`、`pm/strategy_review/hypotheses.json`

### 步骤 1 — Python 环境 ✅
- `.venv\Scripts\python.exe` 存在

### 步骤 2 — 企微 webhook ✅
- `config.local.yaml` 已配置

### 步骤 3 — DB ✅
- `sim_live_mirror.db` 存在

### 步骤 4 — 冒烟 ✅

#### 参数确认
```
bank_swing_strategy: min_net_rr=1.0 min_score_buy=3 max_volume_ratio=0.95 executable_types=['A','B','C','D'] single_budget=8000.0
swing_strategy(#3):  min_net_rr=1.2 min_score_buy=5 max_volume_ratio=0.8 executable_types=['A','B']
```

#### pytest（15/15 全绿）
- `tests/test_swing_fee_budget.py` — 5 passed
- `tests/test_bank_swing_daily.py` — 10 passed（含新增 6 个用例：参数验证、run_scan 传参、单 B 信号通过、量比阈值、swing_auto.PARAMS 级联、config.yaml 银行段比 #3 松）
- `tests/test_swing_params.py` — 6 passed（#3 行为不变）

#### bank_swing_daily.py --no-push --no-trade ✅
- 退出码 0，无 traceback
- 银行日报 `output/bank_swing_daily/2026-09-08.md` 已生成
- 给出挂单建议：**建设银行**(601939) @ 10.68、**交通银行**(601328) @ 7.28
- 不再空仓观望

### 步骤 5 — schtasks ✅
- `QuantLearn_BankSwingDaily` 已存在（16:08），无需重建
- 全部 16 条 QuantLearn 任务均在

### 步骤 6 — OpenClaw cron ✅
- 当前 session 正常，无需操作

## 验收清单

| 检查项 | 结果 |
|--------|:----:|
| 银行参数打印 `1.0 3 0.95 ['A','B','C','D'] 8000.0` | ✅ |
| pytest 全绿（15+6=21 个相关用例） | ✅ |
| 银行日报有挂单建议（建设银行、交通银行） | ✅ |
| 账户 #3 参数不变（min_score_buy=5, max_volume_ratio=0.8） | ✅ |
| schtasks 无需重建 | ✅ |
| 假设 H-003 已登记（review_after=2026-09-29） | ✅ |

## 假设台账
- **H-003**（open）：#4 银行仓 min_score_buy 5→3、量比 0.8→0.95，预期 10 个交易日内至少出现 1 笔模拟买入
