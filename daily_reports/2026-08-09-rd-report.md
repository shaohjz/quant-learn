# 研发 / PM 日报 — 2026-08-09（周日）

> 视角：研发 / PM。今日为非交易日，无 schtasks 交易链路运行；以下快照来自最近交易日 2026-08-07 收盘数据 + 当前未提交改动。

## 1. 代码变更摘要

| 文件 | 状态 | 说明 |
|------|------|------|
| `scripts/pm_daily_report.py` | 未提交（工作区改动） | 被重写为直接读 `data/pm.db` + `pm/BACKLOG.md`。⚠️ 与 `docs/DEPLOYMENT.md`「pm.db 已废除、任务真源为 pm/requirements\|bugs 的 md frontmatter」冲突，疑似回退。需确认是否应丢弃或改回 `pm_store`/md 真源方案。 |
| `scripts/pm_review.py` | 未提交（工作区改动） | 31 行调整，具体 scope 待研发自审。 |
| `scripts/daily_git_sync.py` | 已提交（HEAD~5 内） | 移除 14 行（疑似清理逻辑）。 |
| `scripts/daily_recalibrate.py` | 已提交 | 7 行微调（REQ-105 buy_zone 脏阈值展开）。 |
| `scripts/generate_next_watchlist.py` | 已提交 | 4 行微调。 |
| `scripts/strategy_review.py` | 已提交 | 16 行调整（复盘链静默断更修复）。 |
| `scripts/strategy_review_runner.bat` | 已提交 | 移除 7 行。 |

**最新已入库 commit**：`08fd7aed chore(daily): 2026-08-08 台账/LLM日报/PM 落盘`

> ⚠️ 当前工作区有未提交改动（`pm_daily_report.py` / `pm_review.py` / 多个 `output/*.log` / `dbg*.py` 调试脚本 / `pm_report_gen.py`）。其中 `dbg.py`/`dbg2.py`/`dbg3.py`/`pm_report_gen.py`/`pm_review2.py` 为调试产物，不应入库。

## 2. PM 队列（P0 / P1 / testing）

任务真源：`pm/bugs`、`pm/requirements`（md frontmatter）。Cursor 队列 `pm/cursor_queue/` 今日无新文件。

### P0（阻塞 / 需立刻处理）
- **P0 — `pm_daily_report.py` 回退风险**：当前未提交版本依赖 `data/pm.db`（已废除）。若误提交会重新引入「pm.db 真源」反模式，与部署铁律冲突。建议：研发自审后改为走 md frontmatter；或直接 `git checkout` 丢弃。

### P1（重要，本周内）
- **P1 — 复盘链稳定性**：`strategy_review` / `strategy_scorecard` 在 08-07 后无新产出（周末无运行属正常）。需确认 08-10（周一）16:35 `QuantLearn_StrategyReview` + 16:30 `StrategyScorecard` 恢复正常出数。
- **P1 — 清理调试脚本**：`scripts/dbg.py`/`dbg2.py`/`dbg3.py`/`pm_report_gen.py`/`pm_review2.py` 为临时调试文件，应加入 `.gitignore` 或删除，避免误入库。

### Testing / 验收中
- `capital_status.py` 一致性 ✅：#1 / #3 与 config 全部一致（无资金漂移）。
- `apply_strategy_params.py --show`：#3 `max_positions=5` 应保持（上次变更生效）。

## 3. 阻塞项

| 阻塞 | 影响 | 建议 |
|------|------|------|
| `pm_daily_report.py` 与部署铁律冲突 | 若入库会复活 pm.db 依赖 | 周一前由研发裁决：改回 md 真源 or 丢弃改动 |
| 调试脚本未清理 | 污染 git 工作区 / 误 push 风险 | 加 `.gitignore` 或本地删除 |
| 周末无新行情 | #1/#3 盈亏停留在 08-07 快照 | 正常，周一开盘后刷新 |

## 4. 模拟盘快照（截至 2026-08-07 收盘）

### #1 学习仓（learn）
- 总资产 **95,170.87**（较期初 -4.83%）；现金 40,413；持仓 5 只（仓位上限已提至 8）。
- 持仓：华勤技术 +803(+10.89%)、工业富联 +913(+7.18%)、天赐材料 +468(+4.13%)、中国人寿 -101(-0.87%)、江苏新能 -72(-0.74%)。
- 当日 +221.58（+0.23%）。

### #3 波段仓（swing_trade）
- 总资产 **48,965.00**（较期初 -2.07%）；现金 5,741；持仓 5 只（满仓上限）。
- 持仓全红：国泰海通 -365(-3.92%)、中国太保 -222(-2.30%)、比亚迪 -207(-2.25%)、三一重工 -176(-2.20%)、东方财富 -40(-0.50%)。
- 当日 -60.00（-0.12%）。

> 波段仓 5 只全部浮亏，需关注是否触及跟踪止损（见 finance-report）。

## 5. 明日（2026-08-10 周一）优先

1. **P0 裁决** `pm_daily_report.py`：确认走 md 真源，禁止依赖 pm.db；或丢弃改动。
2. **清理** `dbg*.py` / `pm_report_gen.py` 等调试脚本（`.gitignore` 或删除）。
3. 周一全链路回归：08:40 SwingPool → 09:35 QuantPulse → 16:05~16:35 收盘/复盘/记分卡 → 18:45 + 20:30 DailyGitSync。
4. 验证 08-10 收盘后 `strategy_review` / `strategy_scorecard` 重新出数。
5. 关注 #3 波段仓 5 只浮亏是否触发止损（国泰海通 -3.92% 逼近 -5% 硬止损）。

---
_生成：OpenClaw 日报 Agent（提示词 C）· 2026-08-09 20:02 Asia/Shanghai · 非交易日，快照为最近交易日 08-07。_
