# 运维巡检报告 — 2026-06-12

**巡检时间：** 2026-06-12 19:00 (Asia/Shanghai)  
**执行 Agent：** ops-agent-daily (cron: 7835f916-cb1f-485e-863a-adcd4857653e)  
**报告状态：** ⚠️ 发现问题

---

## 1. 行情源检查  ⚠️ DEGRADED

| 项目 | 状态 | 详情 |
|------|------|------|
| AKShare 实时行情 | ❌ 异常 | `Connection aborted, Remote end closed connection without response`（连续第4天） |
| BaoStock 备选行情 | ✅ 正常 | login success, query success |
| 最新 CSV 数据时间 | ✅ 正常 | 2026-06-12 15:32（收盘数据，非实时时段） |
| `fetch_data.py` 自动恢复 | ✅ 成功 | BaoStock 备选机制工作正常 |

**结论：** 主行情源（AKShare）持续连接失败（连续第4天），`stock_zh_a_spot_em()` 接口远端无响应。BaoStock 备选方案正常接管。

**严重程度：** S1（主源失效，备选可用）

**趋势：** 与 2026-06-09/10/11 报告相同问题，AKShare 仍未恢复。已连续4个交易日主源不可用。

---

## 2. 交易 API 检查  ❌ FAIL

| 项目 | 状态 | 详情 |
|------|------|------|
| `realtime_price_gateway` 导入 | ❌ 失败 | `No module named 'vnpy'` |
| `qmt_gateway` 导入 | ❌ 失败 | `No module named 'vnpy'` |
| `xtquant` 导入 | ❌ 失败 | Python 3.14 不兼容（需 3.11） |
| vnpy 安装状态 | ❌ 未安装 | `.venv` 中无 `vnpy` 包 |
| xtquant 安装状态 | ❌ 未安装 | `.venv` 中无 `xtquant` 包 |

**结论：** 交易网关（QMT/vnpy）依赖缺失，`vnpy` 未安装在虚拟环境中。当前 `broker.mode: sim`，不影响模拟交易，但阻碍实盘交易功能。

**严重程度：** S2（功能缺失，当前 sim 模式影响有限）

**关联历史：** BUG-20260610-002 已记录此问题，根因为 Python 3.14 与 xtquant 不兼容。

---

## 3. 数据库检查  ✅ PASS

| 数据库文件 | 可读 | 可写 | 路径 | 最新写入时间 |
|-----------|------|------|------|------------|
| `data/pm.db` | ✅ | ✅ | data/ | 2026-06-12 19:03 |
| `data/sim.db` | ✅ | ✅ | data/ | 2026-06-12 19:03 |
| `data/sim_live_mirror.db` | ✅ | ✅ | data/ | 2026-06-12 19:03 |

**结论：** 所有数据库文件均可正常读写，无损坏。写入测试通过。

---

## 4. 磁盘 & 内存检查  ✅ PASS

| 项目 | 使用量 | 阈值 | 状态 |
|------|--------|------|------|
| 磁盘 C: | 68.13% (101.56 GB / 148.75 GB) | < 80% | ✅ 正常 |
| 磁盘 D: | 36.70% (36.7 GB / 100 GB) | < 80% | ✅ 正常 |
| 内存 | 60.87% (9.6 GB / 15.75 GB) | < 85% | ✅ 正常 |
| 空闲 C: 空间 | 47.5 GB | — | ✅ 充足 |

**与昨日对比：**
- C: 64.6% → 68.13%（+3.5%，正常波动）
- 内存: 62.3% → 60.87%（略有改善）

**结论：** 磁盘和内存均在安全范围内。

---

## 5. Cron 任务检查  ✅ PASS

| 任务 | 调度 | 状态 | 上次运行 | 运行时长 |
|------|------|------|---------|---------|
| `ops-agent-daily` | `0 19 * * 1-5` (Asia/Shanghai) | ✅ 启用 | 2026-06-12 19:00（本次） | 运行中 |
| 上次运行状态 | — | ✅ ok | `lastRunStatus: ok` | `consecutiveErrors: 0` |

**诊断信息：**
- `lastRunStatus`: `ok`（昨日修复后正常）
- `consecutiveErrors`: 0（错误计数已清零）
- `lastDurationMs`: 202348（约 3.4 分钟，正常）
- `nextRunAtMs`: 1781262121597 → 2026-06-13 19:00（下一个交易日）

**结论：** Cron 任务配置正确，定时触发正常，上次运行成功（昨日 `delivery.mode=none` 问题已通过 report 文件解决）。

---

## 6. 发现问题汇总

| # | 问题 | 严重等级 | 状态 | 首次发现 | 持续天数 |
|---|------|---------|------|---------|---------|
| 1 | AKShare 行情源连接失败，主源持续不可用 | S1 | ⚠️ 备选可用 | 2026-06-09 | **第4天** |
| 2 | `vnpy`/`xtquant` 未安装，交易网关无法加载 | S2 | 待修复 | 2026-06-11 | 第2天 |
| 3 | Python 3.14 与 xtquant 不兼容（根本原因此问题） | S2 | 已知 | 2026-06-10 | 第3天 |

> 问题 2 和 3 为同一根本原因的两种表现，合并处理。

---

## 7. 自动恢复尝试

### 尝试 1：AKShare 恢复（S1）

```python
import akshare as ak
df = ak.stock_zh_a_spot_em()
```

**结果：** ❌ 同样 `Connection aborted` — AKShare 远端问题，非本机网络问题。  
**备选方案：** `fetch_data.py` 已内置 BaoStock 自动切换，工作正常。

**建议：** AKShare 连续4天不可用，已超出临时性故障范围。建议：
1. 将 BaoStock 升为主源，AKShare 降为备选
2. 或增加 Tushare 作为第二主源（需 token）
3. 检查 AKShare GitHub 是否有 API 变更公告

### 尝试 2：vnpy/xtquant 安装（S2）

```bash
.venv/Scripts/pip.exe install vnpy vnpy-qmt
```

**结果：** ⏳ 不适用 — 根本问题是 Python 3.14 与 xtquant 编译版本不兼容，即使安装 vnpy 也无法加载 xtquant。  
**真正解法：** 需要安装 Python 3.11 并创建独立 venv，或禁用 QMT 实盘功能（当前 sim 模式不需此功能）。

---

## 8. 建议行动

### 立即（S1）
1. **AKShare 长期失效**：将 BaoStock 升为主行情源，修改 `realtime_price_gateway.py` 默认 source 为 `baostock`
2. 检查 AKShare GitHub Issues 是否有 East Money API 变更（可能是 `stock_zh_a_spot_em` 依赖的东方财富接口调整）

### 短期（S2）
3. **Python 3.11 环境**：如下个交易周需启用 QMT 实盘，安装 Python 3.11 并创建 `venv_qmt`
4. 如不需要 QMT，在配置中明确禁用，避免导入失败报错

### 配置优化（S3）
5. **Cron 报告投递**：考虑将 `ops-agent-daily` 的 `delivery.mode` 改为 `announce`，以便巡检结果推送

---

## 9. 趋势对比（近4天）

| 日期 | AKShare | 数据库 | 磁盘 | 内存 | Cron | 新问题 |
|------|---------|--------|------|------|------|--------|
| 06-09 | ❌ | ✅ | ✅ | ✅ | ⚠️ | AKShare 首次失效 |
| 06-10 | ❌ | ✅ | ✅ | ✅ | ❌ | xtquant 导入失败 |
| 06-11 | ❌ | ✅ | ✅ | ✅ | ⚠️ | delivery.mode=none |
| 06-12 | ❌ | ✅ | ✅ | ✅ | ✅ | — |

**正向趋势：** Cron 任务状态恢复正常，内存使用略有改善。

---

**巡检完成时间：** 2026-06-12 19:05  
**执行结果：** OPS_DONE（有异常，详见上文）

> AKShare 已连续4天不可用，建议升级为 P1 问题并安排永久切换。
