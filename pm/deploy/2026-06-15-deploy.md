# 部署日志：2026-06-15

**部署时间**: 2026-06-15 18:34 - 18:40 (Asia/Shanghai)
**部署 Agent**: release-agent (quant-finance-manager)
**部署版本**: master @ 53cc10b (merge dev → master)

---

## 部署范围

### 需求（done → deployed）

| 需求 ID | 标题 | 测试报告 | 状态 |
|----------|------|-----------|------|
| REQ-001 | 账户资金口径一致性检查 | TEST-2026-06-15-001 | ✅ deployed |
| REQ-002 | 完整采样（OHLCV 完整性） | TEST-2026-06-15-002 | ✅ deployed |
| REQ-003 | K线数据质量监控 | TEST-2026-06-10（历史） | ✅ deployed |
| REQ-004 | 每日扫描通知 | TEST-2026-06-15-002 | ✅ deployed |
| REQ-005 | 总资产显示修复 | TEST-2026-06-15-002 | ✅ deployed |

### Bug（verified → deployed）

| Bug ID | 标题 | 状态 |
|--------|------|------|
| BUG-001 | 学习账户资金口径跳变 | ✅ deployed |
| BUG-002 | 盘中采样覆盖不完整 | ✅ deployed |
| BUG-003 | 采样数据不足 | ✅ deployed |
| BUG-004 | 真实账户仓位过于集中 | ✅ deployed |
| BUG-005 | 双账户不一致 | ✅ deployed |
| DATA-20260609-001 | AKShare 接口连接中断 | ✅ deployed |

---

## 部署步骤

### 1. Git 操作

```bash
# 保存 dev 分支未提交更改
git stash

# 切换到 master
git checkout master

# 合并 dev（含今天测试通过的代码）
git merge dev

# 解决 pm 文档冲突（6 个文件，均使用 dev 版本）
git checkout --theirs pm/bugs/BUG-003-*.md ...
git add .
git commit -m "merge: dev → master (2026-06-15 部署合并，解决 pm 文档冲突)"

# 推送到远程
git push origin master
```

**合并 commit**: `53cc10b` — merge dev → master

### 2. Staging 冒烟测试

| 测试项 | 结果 |
|--------|------|
| `SimEngine(account_id=1)` 初始化 | ✅ PASS |
| `get_account(use_config_initial_cash=True)` 返回 total_value | ✅ PASS |
| `daily_settle()` 正确计算 total_value | ✅ PASS |
| `fetch_stock_data('600519', ...)` BaoStock 数据源 | ✅ PASS（4 rows, login success） |
| `backfill_data` 模块导入 | ✅ PASS |

### 3. Production 验证

| 验证项 | 结果 |
|--------|------|
| SimEngine 初始化 + 账户一致性校验 | ✅ PASS |
| daily_settle 计算 total_value | ✅ PASS（total_value=9800.0）|
| backfill_data BaoStock 主力数据源 | ✅ PASS |
| git push origin master 成功 | ✅ PASS |

---

## 代码变更摘要（dev 相对于 master）

```
1077a94 docs: 2026-06-15 测试验收通过 - REQ-001/002/004/005 done + 测试报告
49f502e fix: AKShare数据源持续失败修复 + vnpy可选导入
12f31e6 chore: 2026-06-11 日常产出文件提交（复盘报告、扫描结果、pm.db）
```

### 关键文件变更

| 文件 | 变更说明 |
|------|----------|
| `sim/engine.py` | 账户一致性校验、total_value 计算、跳变检测阈值 50%→20% |
| `sim/db.py` | `get_conn()` 动态检查环境变量、账户事件记录 |
| `scripts/backfill_data.py` | BaoStock 主力 + AKShare 备选、指数退避重试 |
| `sim/notifier.py` | 企微通知（新增）|
| `scripts/morning_scanner.py` | 集成 notifier |
| `scripts/daily_review.py` | 集成 notifier、K线不足时降级策略说明 |

---

## 回滚计划

如部署后 30 分钟内出问题：

```bash
# 回滚到上一个 production 版本
git checkout master
git reset --hard 46bd65d  # 上一个 production commit
git push origin master --force
```

**上一个 production commit**: `46bd65d` (chore: sync staged changes)

---

## 状态更新记录

- `pm/requirements/REQ-001.md`: done → deployed
- `pm/requirements/REQ-002.md`: done → deployed
- `pm/requirements/REQ-003.md`: done → deployed
- `pm/requirements/REQ-004.md`: done → deployed
- `pm/requirements/REQ-005.md`: done → deployed
- `pm/bugs/BUG-001.md`: verified → deployed
- `pm/bugs/BUG-002.md`: verified → deployed
- `pm/bugs/BUG-003.md`: verified → deployed
- `pm/bugs/BUG-004.md`: verified → deployed
- `pm/bugs/BUG-005.md`: verified → deployed
- `pm/bugs/BUG-DATA-20260609-001.md`: verified → deployed

---

## 部署结论

✅ **部署成功**

- master 已合并 dev，已推送到 origin
- staging 冒烟测试全部通过
- production 验证全部通过
- 状态文件已全部更新为 `deployed`
- 部署日志已写入 `pm/deploy/2026-06-15-deploy.md`

**下次部署窗口**: 根据测试通过情况另行安排。
