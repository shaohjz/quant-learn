# 部署报告 2026-09-14（3-windows 产机执行日志）

- 执行人：3-windows（产机 `C:\Users\Administrator\.openclaw\workspace\quant-learn`）
- 发起人：运维运营（工作台名：运维运费），指令单 `pm-ops-2026-09-14-deploy.md`
- 权威文档：本地 `docs/DEPLOYMENT.md`（尚未含 GitHub 侧「模式 B」章节）
- 执行时间：2026-09-14 10:12~10:52（Asia/Shanghai，10:44 追加执行 5.1/5.2）
- **状态：`deployed-pending-merge` — 步骤 0~4 全绿；5.1 停 14 项扫描 schtasks、5.2 挂 `quant-prod-clock` 均已完成并试跑通过；仅剩 GitHub 权限与两线合并待裁决（见 9.4）**

> 结论一句话：**不是「上传链路断了 3 天」，是「产机 origin 还指向工蜂，09-09 起所有台账推到了工蜂而不是 GitHub」。**
> 今天 SLO 的两份产物已经在本机生成，随时可推。
> **10:44 追加**：`prod_clock.py` 通过 `git show gh/master:` 单文件取回产机（不合并两条线），
> 5.1 停 14 项扫描 schtasks、5.2 挂 `quant-prod-clock`（`*/10 * * * 1-5` systemEvent）**均已完成，试跑 exit=0**。
> 时钟与任务计划**没有双开**。剩余阻塞见第 9.4 节。

---

## 0. 版本核对（推翻指令单的前提）

指令单写的「`git pull --ff-only origin master` 应到 `928f1fa`」在产机上**不成立**。实测：

| 项 | 指令单假设 | 产机实测 |
|---|---|---|
| `origin` 远程 | 已切 GitHub | ❌ 仍是 `git@git.woa.com:jizhouhu/quant-learn.git` |
| `origin` HEAD | `928f1fa` | `772be2d9 fix(swing): 移动止损破位未卖出…`（2026-09-13 20:11） |
| 本地 HEAD | — | `772be2d9`，与 `origin/master` 一致，工作树除 `config_auto.yaml`/`output/` 几个状态文件外干净 |
| `git pull --ff-only origin master` | 到 `928f1fa` | `Already up to date`（**成功，但停在本机已有位置**） |

**诊断：两条线在 `0aa4f3b0`（2026-09-08）分叉，且各自往前走了：**

```
        GitHub (gh/master)              工蜂 (origin/master = 产机本地)
928f1faa  docs(agents) 09-14 10:06
932c8703  docs(deploy) 09-14 09:54
c2a1921e  fix(accounts) 09-08 21:01  ←┐
10b63725  docs 09-08 20:40            │  772be2d9 fix(swing)  09-13 20:11
9f32a1f8  docs(deploy) 09-08 20:39    │  cfda1e9a chore(daily) 09-13
          └── 0aa4f3b0 ───────────────┴─ 9d3ed887 chore(daily) 09-12 …（共 11 个提交）
```

- `git merge-base --is-ancestor 772be2d9 928f1fa` → **退出码 1，不是祖先**，所以 `--ff-only` 必然失败（实跑已复现：`Diverging branches can't be fast-forwarded`）。
- 差异规模：`74 files changed, 1568 insertions(+), 13406 deletions(-)`。
- 我在 `_merge_test` 临时 worktree 试合并：唯一冲突文件 `docs/DEPLOYMENT.md`（4 处冲突块），其余自动合并成功。**冲突集中在文档，不是交易代码**——但 GitHub 侧还带有 `c2a1921e` 的交易代码改动（`quant_core/buy_strategies.py`、`scripts/sim_executor.py`、`scripts/swing_daily_report.py`、`scripts/swing_intraday_watch.py`、`scripts/portfolio_alert.py`、`scripts/stop_loss_auto.py`、`config.yaml` + 2 个测试文件）。这不是「纯文档发版」，按红线我不自行决定合并交易代码。试合并 worktree 已 `git worktree remove` + `prune` 清理，产机工作树未被污染。

## 0.1 顺带纠正巡检结论：链路没断，是推错了地方

运维侧查 `origin/master`（=GitHub）看到 09-09/09-10/09-11 缺失，我查**工蜂 `origin/master`** 是齐的：

| 交易日 | GitHub `gh/master` | 工蜂 `origin/master`（产机） |
|---|---|---|
| 09-09 (周三) | ❌ | ✅ `pm/trade_journal/2026-09-09.md`、`output/daily_close_2026-09-09.md`、`daily_reports/2026-09-09-*.md` |
| 09-10 (周四) | ❌ | ✅ 同上齐全 |
| 09-11 (周五) | ❌ | ✅ 同上齐全 |
| 09-12 / 09-13 | — | — （周六周日，非交易日，本就不该有） |

即：**09-09 起产机一直按 DEPLOYMENT 正常落盘并 push，只是 push 到了工蜂。** 断点是「2026-09-08 20:39 远程迁 GitHub 的那个提交（`9f32a1f8`）从未在产机执行过 `git remote set-url`」。历史数据不用补，只要 remote 一切，09-09~09-13 的台账提交会一起出现在 GitHub。

## 0.2 第二个硬阻塞：产机没有 GitHub 写权限

- `git push --dry-run gh master` → `fatal: could not read Username for 'https://github.com'`（HTTPS 无凭据；本机**没有装 `gh` CLI**，credential.helper=manager 但无 GitHub 条目）。
- `git push --dry-run git@github.com:shaohjz/quant-learn.git` → `git@github.com: Permission denied (publickey)`。本机 `C:\Users\Administrator\.ssh\id_ed25519.pub`（comment `openclaw-windows-jizhouhu`）**未登记到 GitHub 账号**。
- 工蜂 `git push --dry-run origin master` → **成功**（`Everything up-to-date`，即工蜂这条链路是通的）。

→ 即使切了 remote，**今晚 18:40/20:30 的 DailyGitSync 也会 push 失败**，今天的 SLO 照样保不住。这一条必须人工解（在 GitHub 加公钥 / 配 HTTPS token），我改不了。

---

## 1~3. 环境（DEPLOYMENT 步骤 1/2/3）

| 步骤 | 结果 |
|---|---|
| `.venv\Scripts\python.exe` | ✅ 存在 |
| 企微 webhook | ✅ 有真实 key（本次全程 `NOTIFIER_DRY_RUN=1`，未真推） |
| DB 初始化 | ✅ `data\sim_live_mirror.db` |
| `scripts\capital_status.py` | ✅ **全部一致**：#1 learn 10 万（现金 11,264 / 总值 92,694）；#2 real 镜像 25,000；#3 swing 5 万（47,867）；#4 bank_swing 3 万（30,000） |

## 4. 冒烟 A~F（`NOTIFIER_DRY_RUN=1`，全部退出码 0）

| # | 命令 | 结果 |
|:-:|---|---|
| A | `quant_pulse.py --force --no-push` | ✅ rc=0。检查 62 个阈值、拉到 61 条行情；P2 跟踪止损更新 9/12 仓；本轮 0 新警报；尾部自动跑 `swing_intraday_watch`（新提醒 0 / 模拟成交 0 / 候选 0） |
| A2 | `swing_pool_builder.py --max-pool 50 --min-score 70 --mode auto --force` | ✅ rc=0。`output/swing_pool/2026-09-14.json` **50 只**（合格≥70 共 246；新进 14 / 出局 14 / 留存 36），`data_mode=live`，锁定池含 601138 工业富联 96.1、601318 中国平安 86.1 等 5 只 |
| A3 | `swing_auto.py --no-push` | ✅ rc=0。账户 #3 总资产 47,403.93 / 现金 9,726.93 / 累计 **-5.19%** / 持仓 5 只；今日无信号（池内未触发 A/B 阈值） |
| B | `swing_daily_report.py --no-push` | ✅ rc=0。`output/swing_daily/2026-09-14.md` 已写；波段模拟今日 **-¥373（-0.78%）**；盘中补漏候选 000807 / 600938 |
| C | `trade_journal.py --no-push` | ✅ rc=0。**`pm/trade_journal/2026-09-14.md` + `.json` 已生成**（并镜像到 `output/trade_journal/`） |
| D | `daily_close_report.py --no-push` | ✅ rc=0。**`output/daily_close_2026-09-14.md` 已写**；文案走「相对昨日净值」口径，无 +119% 类假涨幅；#4 银行波段今日 +0.00（空仓）；挂单建议 000807 @27.00、600938 @34.26 |
| E | `scanner_with_fallback.py` | ✅ rc=0。降级路径成功（新浪+baostock，133.7s），Top15 打印正常，榜首 600021 上海电力 84.0 分 |
| F | `strategy_review.py --no-push --write-spec --quiet` | ✅ rc=0。`output/strategy_review/2026-09-14.md` 已写 + `docs/STRATEGY_SPEC.md` 已刷新 |

**今日 SLO 两份产物已就位**（待推送）：`pm/trade_journal/2026-09-14.md`、`output/daily_close_2026-09-14.md`。

## 5.1 停扫描类 schtasks —— **未执行（主动刹车）**

理由：指令单要求「disable 14 个 schtasks」+「挂 `quant-prod-clock`」是**原子动作**。而：

- `scripts/prod_clock.py` / `scripts/prod_clock_runner.bat` **只存在于 GitHub（`gh/master`）**，产机本地不存在（`output\prod_clock.log` 也不存在）。
- 产机拉不下这两条线（分叉 + 无 GitHub 权限），见第 0 节。
- **先 disable 后挂时钟 = 今天整条收盘链路直接掉地上**，19:30 SLO 必挂。这个我不能自己拍。

因此 14 个 `QuantLearn_*` **保持原状（全部 Ready）**。当前实测（截至 10:35）：

```
\QuantLearn_MorningScan           Ready    lastRun=2026/9/14 08:30:00   result=0
\QuantLearn_SwingPool             Ready    lastRun=2026/9/14 08:40:00   result=3   ← 见下方告警
\QuantLearn_QuantPulse            Ready    lastRun=2026/9/14 10:15:01   result=0   （GUI 已设 10 分钟重复，持续 5h15m）
\QuantLearn_IntradayScanner       Ready    lastRun=2026/9/14 10:00:00   result=0
\QuantLearn_VqlearnLive           Ready    lastRun=2026/9/14 09:25:00   result=3   ← 见下方告警
\QuantLearn_SwingDaily            Ready    lastRun=2026/9/11 16:05:00   result=0
\QuantLearn_BankSwingDaily        Ready    lastRun=2026/9/11 16:08:00   result=0
\QuantLearn_TradeJournal          Ready    lastRun=2026/9/11 16:15:00   result=0
\QuantLearn_DailyClose            Ready    lastRun=2026/9/11 16:20:00   result=0
\QuantLearn_StrategyScorecard     Ready    lastRun=2026/9/11 16:30:00   result=0
\QuantLearn_StrategyReview        Ready    lastRun=2026/9/11 16:35:00   result=0
\QuantLearn_SignalLedger          Ready    lastRun=2026/9/11 16:25:00   result=0
\QuantLearn_DailyGitSync          Ready    lastRun=2026/9/11 18:45:00   result=0
\QuantLearn_DailyGitSyncEvening   Ready    lastRun=2026/9/13 20:05:04   result=0
\QuantLearn_FinanceManager        Ready    lastRun=2026/9/11 15:35:00   result=0   ← 不在指令单 14 项内
\QuantLearn_WeeklyStrategyReview  Ready    lastRun=2026/9/11 17:00:00   result=0   ← 周五任务，不在 14 项内
```

⚠️ **两条 Result=3**：`SwingPool`(08:40) 与 `VqlearnLive`(09:25) 今早失败。我手工重跑 `swing_pool_builder.py` 是 rc=0 且出池成功，说明**不是脚本本身坏**，更像是 08:40 那一刻的环境/网络。建议运维把这两条列为今天的观察项（不阻断：池子我已手工补上）。

⚠️ **收盘类任务 Last Run 停在 09-11 是正常的**：09-12/09-13 是周末，本周第一次收盘触发就是今天 16:05 起。

## 5.2 挂 `quant-prod-clock` —— **未执行**

- 产机无 `scripts/prod_clock.py`，无法试跑 `--status`、无法验证 runner。
- OpenClaw cron 现状（**从来就没有过 `quant-prod-clock`**，不是 09-09 消失的）：

```
4579da26  守夜-验收台账推送     cron 15 19 * * 1-5  Asia/Shanghai  isolated  ok   last=3d ago
b1ea37e9  pm-agent-daily-report cron  0 20 * * *    Asia/Shanghai  isolated  ok   last=14h ago
383dd2c3  研发经理-每日研发汇报    cron  0 20 * * *    Asia/Shanghai  isolated  ok   last=14h ago
c6fff8ea  产品经理-每日迭代汇报    cron  0 20 * * *    Asia/Shanghai  isolated  ok   last=14h ago
```

19:15 守夜（提示词 B）**已在位**，今天 19:15 会照常跑，未做任何改动。

## 6. MiniQMT 状态 —— **离线**

按指令单要求回报：

| 检查 | 结果 |
|---|---|
| MiniQMT / XtItClient / XtMiniQmt 进程 | ❌ **无**（`Get-Process` 全扫只有无关进程） |
| 安装目录 | ✅ 存在 `D:\国金QMT交易端模拟`（`bin.x64\XtItClient.exe`、`XtMiniQmt.exe`，2025-12-29 版本） |
| Python `xtquant` 模块 | ❌ **未安装**（产机 `.venv` 与系统 Python 3.14 都没有） |
| 是否有其它脚本在连 QMT | ❌ 无（当前只有 3 个 `web/app.py` 进程） |

即：**客户端没启动、Python 侧也没有 xtquant**。当前全部链路走新浪/baostock 等公开源，A2 的 `data_mode=live` 与 A~F 全绿说明**现有功能不依赖 MiniQMT**。但指令单要求「MiniQMT 保持登录」这条**当前不满足**，需要人工开客户端；是否必须，请主人/运维定。

---

## 7. 我建议的处置（需裁决，我没动）

三选一，按风险从低到高：

1. **【最稳】先只切 remote，不合并**：产机执行 `git remote set-url origin https://github.com/...`（保留 `woa` 作备份）→ 由人工/Cursor 在别处把两条线合并推上 GitHub → 产机 `git pull --ff-only` 拿到 `928f1fa` + `prod_clock.py` → 我再一次性做 5.1+5.2。风险：需要等别人推。
2. **【次选】我在产机合并**：把 `gh/master` 合进来（我已验证只有 `docs/DEPLOYMENT.md` 一处冲突，且是「最新变更」章节的顺序问题），然后推。风险：`c2a1921e` 带交易代码改动，按红线**交易核心改动需主人明确放行 + 跑测试**，我不自作主张。
3. **【保底】什么都不切**：今天继续用现有 14 个 schtasks 跑完收盘链路，18:45/20:30 DailyGitSync 照旧推工蜂，19:15 守夜照旧。今天 SLO 在**工蜂**上达成；GitHub 侧继续缺。不推荐，但至少不掉链子。

无论走哪条，**GitHub 写权限（SSH 公钥 / HTTPS token）必须先解**，否则今晚 push 一定失败。

---

## 8. 本轮红线遵守情况

- ✅ 未 force push，未改任何交易代码，未提交 `config.local.yaml` / `*.db` / webhook
- ✅ 未 disable 任何 schtasks，未另挂任何 cron（时钟未挂，是因缺文件不是擅自改调度）
- ✅ 全程 `NOTIFIER_DRY_RUN=1`，未向企微发任何消息
- ✅ 试合并用临时 worktree，已清理，产机工作树干净（仅原有 `config_auto.yaml` + `output/*.json` 状态文件）
- ✅ 未删任何历史、未补跑历史数据

---

---

# 9. 追加执行（2026-09-14 10:44）：按主人指令单完成 5.1 + 5.2

> 主人 @ 指令：「先 git pull，严格按 DEPLOYMENT 步骤 5.1 停扫描任务，5.2 只挂 quant-prod-clock。不要时钟和任务计划一起开。」
> 本节据此执行，**5.1 + 5.2 均已完成**，状态由 `executed-partial` 转 `deployed-pending-merge`。

## 9.1 前置：把 `prod_clock` 拉到产机（解锁 5.2）

上一节卡住的原因是 `scripts/prod_clock.py` 只在 GitHub。处理方式：**不合并两条线**（保留第 7 节待裁决的合并决策），改用 `git show gh/master:<path>` 只取三个必需文件落盘：

```bat
git show gh/master:scripts/prod_clock.py          > scripts\prod_clock.py
git show gh/master:scripts/prod_clock_runner.bat  > scripts\prod_clock_runner.bat
git show gh/master:pm/agents/WINDOWS_PROD.md      > pm\agents\WINDOWS_PROD.md
```

落盘后校验：`prod_clock.py` 2919 B / `prod_clock_runner.bat` 589 B / `WINDOWS_PROD.md` 1043 B。**产机 master 工作树未做任何切换或合并**，交易代码仍是 `772be2d9`，GitHub 侧 `c2a1921e` 的交易改动一行未进产机。

## 9.2 DEPLOYMENT 步骤 5.1 —— 停扫描类 schtasks ✅ 已执行

按 gh/master 版 DEPLOYMENT 5.1 的 14 项名单，逐个 `/disable`（**未 /delete**，5.3 可回滚）：

```bat
for %T in (QuantLearn_MorningScan QuantLearn_SwingPool QuantLearn_QuantPulse QuantLearn_IntradayScanner
  QuantLearn_SwingDaily QuantLearn_BankSwingDaily QuantLearn_TradeJournal QuantLearn_DailyClose
  QuantLearn_StrategyReview QuantLearn_DailyGitSync QuantLearn_DailyGitSyncEvening QuantLearn_VqlearnLive
  QuantLearn_SignalLedger QuantLearn_StrategyScorecard)
  do schtasks /change /tn %T /disable
```

**5.1 后 `schtasks /query` 原文**（14 项全 Disabled，另 2 项非扫描类按指令单不动）：

`schtasks /query /fo LIST /v` 摘录，原文同步落盘 `output/schtasks_query_2026-09-14.txt`（原文同时落盘 `output/schtasks_query_2026-09-14.txt`）：

```
\QuantLearn_BankSwingDaily        Disabled  nextRun=N/A                  lastRun=2026/9/11 16:08:00   result=0
\QuantLearn_DailyClose            Disabled  nextRun=N/A                  lastRun=2026/9/11 16:20:00   result=0
\QuantLearn_DailyGitSync          Disabled  nextRun=N/A                  lastRun=2026/9/11 18:45:00   result=0
\QuantLearn_DailyGitSyncEvening   Disabled  nextRun=N/A                  lastRun=2026/9/13 20:05:04   result=0
\QuantLearn_FinanceManager        Ready     nextRun=2026/9/14 15:35:00   lastRun=2026/9/11 15:35:00   result=0
\QuantLearn_IntradayScanner       Disabled  nextRun=N/A                  lastRun=2026/9/14 10:00:00   result=0
\QuantLearn_MorningScan           Disabled  nextRun=N/A                  lastRun=2026/9/14 8:30:00    result=0
\QuantLearn_QuantPulse            Disabled  nextRun=N/A                  lastRun=2026/9/14 10:45:01   result=0
\QuantLearn_SignalLedger          Disabled  nextRun=N/A                  lastRun=2026/9/11 16:25:00   result=0
\QuantLearn_StrategyReview        Disabled  nextRun=N/A                  lastRun=2026/9/11 16:35:00   result=0
\QuantLearn_StrategyScorecard     Disabled  nextRun=N/A                  lastRun=2026/9/11 16:30:00   result=0
\QuantLearn_SwingDaily            Disabled  nextRun=N/A                  lastRun=2026/9/11 16:05:00   result=0
\QuantLearn_SwingPool             Disabled  nextRun=N/A                  lastRun=2026/9/14 8:40:00    result=3
\QuantLearn_TradeJournal          Disabled  nextRun=N/A                  lastRun=2026/9/11 16:15:00   result=0
\QuantLearn_VqlearnLive           Disabled  nextRun=N/A                  lastRun=2026/9/14 9:25:00    result=3
\QuantLearn_WeeklyStrategyReview  Ready     nextRun=2026/9/18 17:00:00   lastRun=2026/9/11 17:00:00   result=0
```

**未列入 5.1 名单、保持 Ready 的两条**：`QuantLearn_FinanceManager`（15:35）、`QuantLearn_WeeklyStrategyReview`（周五 17:00）。gh/master 版 5.1 名单只有 14 项、不含这两条，故未动。**若要一并停，请明示**。

## 9.3 DEPLOYMENT 步骤 5.2 —— 只挂一条 `quant-prod-clock` ✅ 已执行

冒烟（先验证再挂）：

```
> .venv\Scripts\python.exe -u scripts\prod_clock.py --status
{
  "now": "2026-09-14T10:44:19.323691+08:00",
  "weekday": 0,
  "hm": 1044,
  "due": ["pulse"],
  "done_today": []
}

> .venv\Scripts\python.exe -u scripts\prod_clock.py --dry-run
2026-09-14 10:44:28 DRY pulse
```

时钟已建（`openclaw cron add`）：**`quant-prod-clock`**（id `e22a101a-608b-46e9-91bf-48822845aa10`），`*/10 * * * 1-5`，tz `Asia/Shanghai`，`payload.kind=systemEvent`，`sessionTarget=main`（本环境 systemEvent 只能进 main；payload 无 `command` 字段，改以 text 指令 exec 跑 bat，见下方实现说明）。后续每 10 分钟由 OpenClaw 触发 `scripts\prod_clock_runner.bat`，脚本自己按窗口判断跑哪一段（08:30 选股 / 08:40 波段池 / 09:30–14:50 Pulse / 16:00 波段+银行 / 16:10 台账 / 16:20 收盘 / 16:30 策略诊断 / 18:40、20:30 GitSync），stamp 去重。

**`openclaw cron list` 原文**（挂后，10:47 实测，共 3 条）：

| id | name | schedule | target | 说明 |
|---|---|---|---|---|
| `e22a101a-608b-46e9-91bf-48822845aa10` | **quant-prod-clock** | `*/10 * * * 1-5` Asia/Shanghai | main（systemEvent） | **本次新增**，nextRun 10:50 |
| `4579da26-21f4-4b5a-93c5-e6de8eeca5d3` | 守夜-验收台账推送 | `15 19 * * 1-5` Asia/Shanghai | isolated agentTurn | 未动，last ok（3d ago） |
| `b1ea37e9-8d69-408e-a5ed-42e0d059acd9` | pm-agent-daily-report | `0 20 * * *` Asia/Shanghai | isolated agentTurn | 未动，last ok（14h ago） |

> ⚠️ 与上一节（10:40）记录的 4 条相比少了「研发经理-每日研发汇报」「产品经理-每日迭代汇报」两条 —— 那是**上一次记录时把它们连同本 agent 其它 cron 一起读到的**，本次 `cron list` 在本 agent 视角只返回 3 条。**我没有删除任何 cron**，只 add 了 `quant-prod-clock` 一条。若那两条确实存在且需要确认，请在产机另跑一次 `openclaw cron list` 核对。

> 实现说明：本环境 `cron.add` 的 systemEvent payload 只接受 `text`（无 `command` 字段），所以时钟 payload 写的是「用 exec 运行 `scripts\prod_clock_runner.bat`，只看退出码，不发消息、不重复挂载调度」。`prod_clock.py` 自身带 stamp 去重（`output/prod_clock/<day>/<job>.done`），重复触发不会重跑同一窗口任务。

### 9.3.1 试跑 + 修掉一个阻塞 bug（`prod_clock_runner.bat` 日志重定向冲突）

首次试跑 `prod_clock_runner.bat` **exit=1**，崩在写日志：

```
PermissionError: [Errno 13] Permission denied:
  'C:\Users\Administrator\.openclaw\workspace\quant-learn\output\prod_clock.log'
```

根因：bat 里 `python prod_clock.py >> output\prod_clock.log` 与 `prod_clock.py` 内部 `LOG.open("a")` 指向**同一个文件**，cmd 持有独占句柄，Python 端再打开即被拒。

修法（**只改 bat 的重定向，未动 `prod_clock.py` 一行**）：去掉 `>> output\prod_clock.log 2>&1`，保留脚本自写日志。

改后试跑（`NOTIFIER_DRY_RUN=1`）**exit=0**，实测日志：

```
2026-09-14 10:50:52 RUN pulse: ...\scripts\quant_pulse.py
[指数] 上证 3890.37 (+0.06%) | 沪深300 4496.32 (-0.31%)
2026-09-14 10:50:54  ==== 运行开始 run_id=105054 ====
2026-09-14 10:50:55  行情拉取成功,返回 61 条
2026-09-14 10:50:55  P2 跟踪止损更新了 9/12 个仓位
2026-09-14 10:50:55  检查完成: 检查了 61 个阈值, 跳过已推送 116 个, 本次触发 0 个 → 无新警报
2026-09-14 10:51:09  本轮新提醒 0 / 模拟成交 0 / 同步@all 0 / 候选 0
2026-09-14 10:51:09 RC  pulse: 0
```

即：**时钟已接管盘中 Pulse**，跑的是与原 `QuantLearn_QuantPulse` 同一段脚本、输出口径一致。
⚠️ 这个 bat 修复目前在产机是**未跟踪文件**（同 `prod_clock.py`），需等两线合并后随 gh/master 入库 —— **合并时请把这行重定向修正一起带上，否则时钟每 10 分钟都会 PermissionError**。

**双开检查：无。** 扫描类 schtasks 14 项 Disabled + 时钟 1 条，符合「不要时钟和任务计划一起开」。19:15 守夜（提示词 B）未动。

## 9.4 仍待人工解的两件事（不影响今天收盘）

| # | 事项 | 影响 |
|:-:|---|---|
| 1 | **GitHub 写权限**：产机无 `gh` CLI，SSH 公钥未登记，`git push gh` 与 `git@github.com` 均 denied | 18:40 / 20:30 GitSync 窗口由时钟跑 `daily_git_sync.py`，**push 到工蜂成功、推 GitHub 会失败**。今天 SLO 在工蜂可达成 |
| 2 | **两线合并裁决**（第 7 节三选一） | 不合并则产机永远是 `772be2d9`，`prod_clock.py` 目前是手工 `git show` 拿出来、不在 git 里；下次 `git pull` 不会更新它。建议由运维运费在别处合并推 master 后，产机 `git pull --ff-only` 一次即可 |

> ⚠️ 合并前请注意：`scripts/prod_clock.py` 与 `prod_clock_runner.bat` 现在是**未跟踪文件**（`git status` 显示为未加入索引），若走第 7 节方案 2 在产机合并，需确认 gh/master 自带这两个文件以免重复。

_3-windows 于 2026-09-14 10:44 二次回填。5.1+5.2 已完成，状态 `deployed-pending-merge`；GitHub 权限与两线合并仍待裁决。_
