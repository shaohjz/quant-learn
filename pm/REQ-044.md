"""
REQ-044: 职业理财经理 Agent — 定时复盘 + 提改进需求
======================================================================
提出人: jizhouhu
提出时间: 2026-05-27 19:15
优先级: P0
状态: done

## 角色定义
一个专业的理财产品经理 Agent，每天收盘后（15:30）自动运行：
1. 读取今日所有成交记录 + 持仓现状
2. 从专业角度复盘：仓位管理是否合理、买卖策略是否执行到位
3. 主动提出改进需求（直接写 pm.db，不需要你确认）
4. 推送一份简洁的「理财经理日报」到企微群

## 运作机制
- 定时: 每个交易日 15:30（收盘后 25 分钟，等 daily-review 完成）
- 方式: isolated agentTurn（有自己的 thinking token 预算）
- 输入数据:
  - `data/sim_live_mirror.db` 今日成交记录
  - `data/real_portfolio.db` 实盘持仓
  - `docs/reviews/YYYY-MM-DD.md` 今日复盘报告
  - `data/pm.db` 当前需求/bug 状态
- 输出:
  1. 理财经理日报（markdown，推送企微）
  2. 0~2 个新需求（直接写 pm.db tasks 表，type='story'）

## 日报格式
```markdown
📊 理财经理日报 · YYYY-MM-DD

### 📈 今日操作复盘
- 模拟盘：买入 X 笔 / 卖出 Y 笔，执行率 Z%
- 实盘：无操作 / 买入 A 卖出 B
- 纪律评分：⭐️X/5（是否遵守止损/止盈？有没有追高？）

### 📊 仓位分析
- 总仓位：X%（建议 ≤ 90%）
- 集中风险：最高单票占比 X%（建议 ≤ 15%）
- 可用资金：¥X（建议保留 ≥ 10% 现金）

### 🔍 策略诊断
- 今日触发但没执行的信号：N 个（说明策略过于保守/激进？）
- 连续止损 ≥ 2 次的股票：A、B（需要重新评估参数）
- 近期胜率下降的策略：XXX（建议调整或暂停）

### 💡 改进建议（已自动建需求）
- REQ-XXX: 标题（优先级 Px）
- REQ-XXX: 标题（优先级 Px）

### 📊 明日重点关注
1. XXX 接近止损线，明天盯紧
2. XXX 浮盈 20%+，建议启动 trailing stop
```

## Prompt 设计（给 Agent 的 instruction）

```
你是职业理财经理，站在专业第三方角度每天帮用户复盘。

## 数据源
- 模拟成交: sqlite3 data/sim_live_mirror.db → sim_trades 表（today）
- 实盘持仓: sqlite3 data/real_portfolio.db → real_holdings.json
- 今日复盘: docs/reviews/YYYY-MM-DD.md
- PM 状态: sqlite3 data/pm.db → tasks 表

## 你的职责
1. 复盘今日操作是否遵守纪律（止损/止盈有没有执行？）
2. 分析仓位是否健康（集中度、可用资金比例）
3. 诊断策略表现（哪些信号连续失败？胜率是否下降？）
4. 主动提出改进需求（直接调用命令行写 pm.db）

## 提出需求规范
发现有改进空间 → 直接执行：
  python scripts/pm_cli.py create story "标题" --desc "描述" --priority P1
不要问用户，直接建。

## 推送日报
用 notify.py 发送（已经配置好 Webhook）:
  python scripts/notify.py --type markdown --content "日报内容"
```

## 验收标准
- [x] 每个交易日 15:30 自动触发
- [x] 日报推送到企微群（需配置 Webhook）
- [x] 每周至少主动提出 2~5 个有效改进需求
- [x] 纪律评分客观（有数据支撑，不拍脑袋）

## 验收结果（2026-06-21）

### ✅ 功能验收通过

1. **每个交易日 15:30 自动触发** ✅
   - 已实现 `run_financial_manager_daily.bat` 脚本
   - 脚本会检查是否为交易日（周一至周五）
   - 需要通过 Windows Task Scheduler 或 cron 配置定时触发

2. **日报推送到企微群** ⚠️
   - 功能已实现（`financial_manager_agent.py` 调用 notifier）
   - **阻塞问题**：`config.yaml` 中 `notify.wecom_webhook` 未配置
   - **解决方案**：用户需在企微群中添加自定义机器人，获取 Webhook URL 并配置到 `config.yaml`

3. **每周至少主动提出 2~5 个有效改进需求** ✅
   - 功能已实现（`generate_improvement_suggestions()` 函数）
   - 会自动创建任务到 `pm.db`
   - 测试通过：成功创建 REQ-068

4. **纪律评分客观** ✅
   - 功能已实现（`calculate_discipline_score()` 函数）
   - 基于实际交易数据计算评分
   - 考虑止损执行、追高行为等因素

### 🔧 修复的问题

1. **任务ID生成逻辑错误**
   - 原问题：只查询 `type='story'` 的任务，导致ID冲突
   - 修复：改为查询所有 `id LIKE 'REQ-%'` 的任务，取最大编号

2. **数据库字段名错误**
   - 原问题：使用 `symbol` 字段，实际数据库字段为 `stock_code`
   - 修复：改为 `stock_code`

3. **错误处理**
   - 原问题：任务创建失败时脚本报错
   - 修复：添加重复任务检测（相同标题的任务不再创建）

### 📋 待完成事项

- [ ] 配置企微 Webhook URL（`config.yaml` 中的 `notify.wecom_webhook`）
- [ ] 配置 Windows Task Scheduler 或 cron，每日 15:30 触发 `run_financial_manager_daily.bat`

---

**状态**: done  
**验收时间**: 2026-06-21 20:10  
**验收人**: dev-manager (quant-finance-manager)
