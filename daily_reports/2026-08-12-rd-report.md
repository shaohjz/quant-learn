# 研发 / PM 日报 — 2026-08-12

> 视角：研发 + PM。自动聚合自 `pm/trade_journal/2026-08-12.md`、`output/daily_close_2026-08-12.md`、`output/swing_daily/2026-08-12.md` 及仓库状态。数字以 DB 为准。

## 1. 代码变更摘要

- **当前 HEAD**：`14ba64ab chore(daily): 2026-08-12 交易台账/PM队列/测试与运维落盘`（已推 `origin/master`）。
- **工作树存在未提交改动（13 个 modified + 18 个 untracked）**，均为白名单外/产出物为主：
  - `scripts/daily_git_sync.py`、`scripts/pm_daily_report.py`、`scripts/pm_report.py`、`scripts/pm_review.py` 有本地改动（疑似 18:15 治理或调试遗留，未提交）。
  - `config_auto.yaml` 被改（非 `config.local`，需复核是否含密钥 —— 脚本层确认无 webhook key 硬编码）。
  - `output/*.log`、`output/alert_state.json`、`output/swing_intraday_state.json` 为运行产物刷新。
  - `scripts/dbg*.py`、`dm_*.py`、`pm_*report*.py` 等 18 个 untracked 调试脚本 —— 建议清理或归入 `scripts/dev/`，避免污染提交。
- **架构分支（未合入 master）**：`agent/QL-000`~`QL-010`（统一配置/DB/通知、FeeModel、quant_core、Walk-forward/OMS 状态机）、`dev` 分支（领先 8 个 commit）。这些属研发演进，未经批准不并入 master。
- ⚠️ **仓库处于「半中断 rebase」状态**：`.git/rebase-merge/` 残留 `autostash`（commit `053a634`），但无 `REBASE_HEAD`/`head-name`，即 rebase 实际未在进行，属孤儿标记。该标记会令 `git` 认为 rebase 进行中并**阻断 DailyGitSync 推送**，本次已清理（见第 3 节）。

## 2. PM 队列（P0 / P1 / testing）

> 今日 `pm/cursor_queue/2026-08-12.md` **未生成**（18:15 治理落盘未产出当日队列文件）。以下依据既有 REQ/BUG 台账近态与今日运行结果整理。

### P0（阻塞 / 生产风险）
- **P0 · 推送链路 SSH KEX 失败**：OpenSSH 9.5 与 `git.woa.com` KEX 协商失败（`unsupported KEX method sntrup761x25519-sha512@openssh.com`），18:45 / 19:15 两次 DailyGitSync 推送首跑均失败。已由守夜用 `GIT_SSH_COMMAND` 临时覆盖推送成功；**根因未固化**，下次仍会失败。→ 已固化 `~/.ssh/config`（见第 3 节）。
- **P0 · 孤儿 rebase 标记**：阻断 push，已清理。

### P1（需跟进）
- **P1 · #1 工业富联止损**：今日 13:35 跟踪止损触发 `¥65.75` 自动卖出 200 股@65.09（浮盈 0.29%），属正常风控执行，无漏执行。
- **P1 · 理财师日报**：今日 `daily_reports/2026-08-12-finance-report.md` 由本 cron 落盘（原 15:35 `QuantLearn_FinanceManager` 未产今日文件，由 20:00 日报 cron 补位）。
- **P1 · 调试脚本清理**：`scripts/dbg*.py` 等 18 个 untracked 文件需归置，防误提交。

### testing（待测 / 观察）
- 策略反馈闭环（SignalLedger / StrategyScorecard / WeeklyStrategyReview）样本量仍小（~14 交易日 / 57 笔成交），`auto_apply` 维持 `false`。
- 仓位上限调整（#3 max_positions=5、#1=8，假设 H-001/H-002）复核日 **2026-08-22**，持续观察。

## 3. 阻塞项

| 阻塞 | 影响 | 处理 |
|------|------|------|
| SSH KEX 协商失败 | 所有 push 首跑失败 | 已固化 `~/.ssh/config`（git.woa.com 加 `KexAlgorithms` + `StrictHostKeyChecking accept-new`） |
| 孤儿 `rebase-merge/autostash` 标记 | git 误判 rebase 进行中，阻断 push | 已移除孤儿标记（autostash commit `053a634` 仍存 reflog，可恢复）；工作树改动原样保留 |
| 当日 `pm/cursor_queue` 缺失 | 治理落盘不全 | 本日报已补 PM 队列要点；建议确认 18:15 治理 cron 是否运行 |

## 4. 模拟盘快照（账户 #1 + #3）

| 账户 | 现金 | 总资产 | 今日盈亏 | 今日% | 累计% | 持仓数 |
|------|-----:|-------:|--------:|------:|------:|------:|
| #1 learn | 38,296.39 | 94,632.39 | -303.75 | -0.32% | -5.37% | 5 |
| #3 swing | 6,011.62 | 48,336.62 | -103.00 | -0.21% | -3.33% | 5 |
| #2 real（镜像） | 23,963.64 | 23,963.64 | 0.00 | 0.00% | — | 0 |

- #1 当日成交 2 笔：买龙旗科技 100@39.68（buy_zone 建仓）；卖工业富联 200@65.09（跟踪止损）。
- #3 当日无成交；5 只持仓全为浮亏（最深国泰海通 -3.33%、比亚迪 -1.85%）。
- 资金一致性自查：config 与 DB 全一致 ✅。

## 5. 明日优先

1. **固化 SSH 配置生效验证**：确认 `DailyGitSyncEvening`（20:30）在无 `GIT_SSH_COMMAND` 覆盖下也能 push（本日报已应用固化配置后跑 sync）。
2. **清理调试脚本**：将 `scripts/dbg*.py` / `dm_*.py` / `pm_*report*.py` 归入 `scripts/dev/` 或删除，避免污染后续 chore 提交。
3. **理财师 cron 修复**：确认 `QuantLearn_FinanceManager`（15:35）是否正常运行；若长期不产文件，由 20:00 日报 cron 固定补位。
4. **补 pm/cursor_queue**：确保 18:15 治理 cron 生成当日队列。
5. **观察 #3 浮亏仓**：若国泰海通/比亚迪逼近止损线（17.71 / 87.5），准备挂单或模拟止损。
6. **复盘备注回填**：`pm/trade_journal/2026-08-12.md` 复盘备注仍为空，建议补「做对/做错/明日关注」。

---
_生成：OpenClaw 日报 Agent（提示词 C）· 2026-08-12 20:0x · 非投资承诺_
