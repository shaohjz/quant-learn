# 研发/PM 日报 — 2026-09-16

> 视角：研发 / PM。来源：今日 git 提交、output/ 与 pm/ 产物、DEPLOYMENT.md。
> 今日 = 2026-09-16（周二，交易日）。本文件由 OpenClaw 日报 Agent 落盘，未经人工改交易核心。

## 一、代码变更摘要

### 已入库（今日 commits，master）
| 提交 | 说明 | 性质 |
|------|------|------|
| `f8ad7de9` | feat(horizon)：三账户切中长线低买高卖，模拟仓可重置重跑 | **交易核心演进**（主人发起，非本 Agent 改动） |
| `63d579f7` | chore(daily)：2026-09-16 交易台账/PM队列/测试与运维落盘 | 纯产物提交（output/pm 24 文件） |

### `f8ad7de9` 关键改动（中长线改造）
- **新增** `quant_core/horizon.py`、`quant_core/position_strategies.py`、`research/position_backtest.py`
- **新增脚本** `scripts/fetch_backtest_bars.py`、`scripts/horizon_runtime.py`、`scripts/run_position_backtest.py`
- **改动** `scripts/daily_recalibrate.py`（buy_zone→60日低点、trend_break→MA60×0.88、ATR 跟踪浮盈 15% 才启动、关半仓止盈）、`scripts/reset_account.py`（默认清 #1+#3+#4）、`scripts/sim_executor.py`、`scripts/swing_daily_report.py`（G 信号/周五开仓）、`scripts/swing_intraday_watch.py`
- **配置** `config.yaml` +76 行：`horizon.enabled`、`swing.execution.stop_loss_pct/take_profit_pct/max_positions`、`learn` 止损 12%/止盈 20%
- **测试** `tests/test_position_strategies.py`、`test_horizon_runtime.py`、`test_bank_swing_daily.py`、`test_swing_daily_report.py` 等新增/更新

### 工作区未提交（待下次 sync 关注）
- `config_auto.yaml` +104 行（疑似自动生成配置，未在白名单关键路径，建议确认是否需入库）
- `docs/STRATEGY_SPEC.md` 刷新（策略说明书自动生成）
- 其余为今日产物（output/pm）随 18:45/20:30 sync 推送

## 二、PM 队列 P0 / P1 / testing

### cursor_queue（显式队列）
- 目录最新文件停留在 **2026-07-28**，无今日新增条目。→ **队列无在途 P0/P1**，PM 队列为空。

### 来自策略复盘的 P0/P1（非队列，但需跟进）
策略复盘报告（`output/strategy_review/2026-09-16.md`）列出 **12 P0 / 2 P1**，其中与本次中长线改造直接相关、需人工补登记的：
- **P0 ×9**：`swing.entry_types`/`stop_loss_pct`/`take_profit_pct`/`max_positions`/`single_budget` 及 `learn.*` 共 9 个参数「被改过但没有对应假设记录」。根因：本次改造走 config 直接改参，未开 `--open-hypothesis`，复盘会持续报 P0。
- **P1**：09-15、09-16 波段池「过稳定分线 0 只却仍入池 50 只」（种子兜底），相关绩效不真实。
- **P1**：#1 学习仓 `single_budget` 参数抽取不到（代码常量改名/迁移）。

### 测试状态
- 今日冒烟（deploy 报告）A~F **全绿**，pytest 套件随 `f8ad7de9` 新增/更新后通过。
- `pm/test_reports/` 目录停留在 2026-06，为历史归档；**当前测试以 `tests/` 为准**，无新 failing。

## 三、阻塞项

1. **⚠️ OpenClaw 日报 LLM cron 频率受限**：`pm-agent-daily-report` 今日连续 5 次 error（rate_limit），导致 20:00 自动日报未能落盘——本次由调度任务兜底手动跑通。建议降频或拆分，避免占用 LLM 配额。
2. **参数变更无假设记录**：中长线改造 9 个参数缺假设，复盘每日报 P0。需主人/后端跑 `strategy_review.py --open-hypothesis` 登记 H-006+，否则该行 P0 不消。
3. **波段池兜底**：连续两日入池 50 只实为种子兜底，绩效统计失真。需 `swing_pool_builder` 在兜底时显式标记 `data_mode=seed`。
4. **DailyGitSyncEvening schtasks 处于 Disabled**：扫描类任务按模式 B 已禁用，依赖 20:30 时钟/手动跑。本次按指令 `schtasks /run` 强制执行推送。

> 红线确认：本 Agent 未改交易核心、未 force push、未提交 *.db / config.local。

## 四、模拟盘快照（2026-09-16 收盘）

三账户今日 **重置清空**（中长线改造第一步），全部空仓，无成交。

| 账户 | 资金 | 总资产 | 今日盈亏 | 累计 | 持仓 | 成交 |
|------|-----:|-------:|--------:|-----:|------|-----:|
| #1 learn（中长线 mr_base） | 100,000 | 100,000 | —（缺昨日净值） | +0.00% | 空仓 | 0 |
| #3 swing（pb_weekly） | 50,000 | 50,000 | — | +0.00% | 空仓 | 0 |
| #4 bank（mr_wide_take） | 30,000 | 30,000 | — | +0.00% | 空仓 | 0 |

- 回测基线（holdout 自 2026-01-05）：#1 mr_base +9.33% / 回撤 8.58%；#3 pb_weekly +19.27% / 8.64%；#4 mr_wide_take +13.77% / 6.39%。（前复权研究数据，非实盘证据）
- 当日 `prod_clock` 已完成：bank_swing / close / git_sync / journal / strategy_review / swing_daily。

## 五、明日优先

1. **下一交易日信号**（非周五则 #3 不开仓）：
   - #1 `mr_base`：关注 60 日低点 + RSI≤40 买点，止损 12%/止盈 20%，最短持有 15 日。
   - #3 `pb_weekly`：**仅周五附近开仓**（价在 MA60 上回踩周线 MA20），3 仓×1.5 万；盘中 Pulse 不再给 #3 买，只处理灾难止损。
   - #4 `mr_wide_take`：银行池低买，每日可扫，止损 15%/止盈 30%。
2. **补登记假设**：对本次 9 个参数变更开 H-006+ 假设，消除策略复盘 P0。
3. **修复波段池兜底标记**：`swing_pool_builder` 兜底时输出 `data_mode=seed`，避免绩效失真。
4. **修复日报 cron 频率限制**：调整 `pm-agent-daily-report` 调度，确保 20:00 自动落盘不被限流。
5. **确认入库**：验证本日报经 20:30 Evening sync 进入 `origin/master` 的 `daily_reports/`。

_— OpenClaw 日报 Agent · 2026-09-16 20:0x (Asia/Shanghai)_
