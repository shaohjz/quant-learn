# 2026-05-25 每日研发闭环

## 状态扭转
本次先执行状态扭转，再提出新需求。

### 需求
- REQ-002 收益率曲线图：`testing -> done`
- REQ-003 持仓饼图 + 统计页增强：`testing -> done`
- REQ-004 实时刷新动画 + 数据更新提示：`testing -> done`

### Bug
- BUG-001 `/api/stats` 返回 JSON 不完整/异常：`fixed -> verified`
- BUG-002 scanner fallback Windows GBK Emoji 崩溃：`fixed -> verified`

## 测试报告
新增：`pm/test_reports/TEST-2026-05-25-regression-001.md`

验证内容：
- 首页 HTTP 200
- `/api/equity_curve` HTTP 200，包含 `dates/returns/benchmark`
- `/api/stats` HTTP 200，包含增强统计字段
- `/api/real_portfolio` HTTP 200，包含 `account/positions`
- HTML 包含 `echarts/equity-chart/pie-chart/flash-up/flash-down/status-indicator/update-time`

## 新增需求
- REQ-008 每日PM自动状态扭转与新需求生成（P0）
- REQ-009 PM需求去重与需求池分层看板（P1）
- REQ-010 研发/测试 Agent 失败自动降级接管机制（P1）

## 风险与改进
- 子 agent 今天多次超时，必须引入失败自动降级机制。
- 每日闭环任务必须先处理状态扭转，否则 PM 汇报会持续复述旧需求。
- 新需求生成前必须做去重。

## 下一步
1. 研发优先实现 REQ-008，保证每日流程自身先稳定。
2. 然后实现 REQ-009，让 PM 看板不再混乱。
3. 最后实现 REQ-010，降低子 agent 失败对流程的影响。
