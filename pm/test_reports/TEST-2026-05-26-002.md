# Test Report REQ-010 and REQ-018

## Verified Items

1. REQ-018: 单票止损止盈监控及预警
   - Verified the logic in `portfolio_alert.py` correctly calculates percentage pnl and generates warning messages for predefined bounds (-15%, -8%, 10%, 20%). 
   - Validated via test case injection bypassing `is_trade_time` bounds to assure trigger works.
   - Status updated to `done`.

2. REQ-010: 研发/测试 Agent 失败自动降级接管机制
   - Verified `scanner_with_fallback.py` includes the timeout and fallback mechanism to `scanner_lite.py`.
   - The runner scripts utilize `scanner_with_fallback.py` to prevent failure loops.
   - Status updated to `done`.
