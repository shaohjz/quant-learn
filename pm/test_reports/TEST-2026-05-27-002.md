# TEST-2026-05-27-002 — REQ-039 P0 智能止损量价共振+收盘确认 验收

**测试日期**: 2026-05-27
**测试 Agent**: QuantLearn 测试 Agent (cron)
**待测**: REQ-039 (testing) — 智能止损 P0：量价共振 + 收盘确认

## 一、本次范围
- REQ-039 P0 ✅ 通过 → done
- REQ-040/041 状态仍为 pending（开发 commit `6c308c8` 已实现，但 PM 未推进至 testing），不在本次回归范围；建议下一个开发周期由需求 Agent 推进流转。

## 二、关键 commit
- `26f0913` feat(stop-loss): REQ-039 P0 三档评级 + 量价确认 + 收盘确认 + 跟踪止损联动
- `6c308c8` feat: REQ-039/040/041 P0+P1+P2 全部实现

涉及文件：
- `scripts/sim_executor.py` — `_check_stop_loss_severity` / `_get_today_vol_ratio` / `calc_trailing_stop` / `update_position_trailing`
- `scripts/portfolio_alert.py` — soft / confirmed / hard 三级 alert + 量能上下文
- `scripts/daily_advisor.py` — 持仓段量比/近收盘标签 + 分级建议
- `scripts/migration_add_trailing_stop.py` — `sim_positions.highest_price` / `trailing_stop_price` 列已添加
- `config.yaml` — ATR 止损配置

## 三、验收用例

### 1. 接口存在性
| 函数 | 结果 |
|---|---|
| `sim_executor._check_stop_loss_severity` | ✅ 存在 (sig: code, rule, cur_price, position) → tuple[severity,label,action] |
| `sim_executor._get_today_vol_ratio` | ✅ 存在 |
| `sim_executor.calc_trailing_stop` | ✅ 存在 |
| `sim_executor.update_position_trailing` | ✅ 存在 |

### 2. 跟踪止损分档计算（calc_trailing_stop）
| 场景 | 输入 (entry, high, cur_ts) | 输出 trailing | 说明 |
|---|---|---|---|
| 浮盈 3% | (10, 10.3, None) | 0.00 | 未启动 ✅ |
| 浮盈 6% | (10, 10.6, None) | 10.00 | 保本位 ✅ |
| 浮盈 15% | (10, 11.5, None) | 10.20 | 锁 2% ✅ |
| 浮盈 25% | (10, 12.5, None) | 11.50 | max(entry×1.10, high×0.92) ✅ |
| 高点回落不下移 | (10, 11.0, 10.5) | 10.50 | 不下移 ✅ |

### 3. 三档评级 — 持仓实测（data/sim_live_mirror.db）
- **600310 广西能源** 当前 5.78、trailing_stop 5.80 → severity=`confirmed`、action=`SELL_HALF`，提示「⏰ 近收盘仍跌破¥5.80 (量比1.48×)，避免拖到明天减半」。验证：跟踪止损位与 rule_stop 取 max、近收盘升级 confirmed、量比标签输出均符合 P0 设计。 ✅
- 其余 8 只持仓未跌破止损价 → severity=`none` / action=`NO_ACTION`，无误报。 ✅

### 4. 数据库字段
- `sim_positions` 表已含 `highest_price`、`trailing_stop_price`（migration 已落地于 `sim.db` 与 `sim_live_mirror.db`）。 ✅
- `sim_live_mirror` 中 600310 已写入 highest_price=6.30 / trailing_stop_price=5.80，与之前 P2 提交的乾坤模拟结论一致。 ✅

## 四、结论

**REQ-039 P0 验收通过 → status: testing → done**

P0 设计要点全部命中：
1. soft（仅预警不下单）/ confirmed（SELL_HALF）/ hard（SELL_ALL）三档判定
2. 量比 ≥ 1.5 升级 confirmed
3. 14:50 后近收盘仍跌破 → 升级 confirmed 避免拖隔夜
4. 跟踪止损位与 rule_stop 取 max（实盘 600310 已验证）

## 五、遗留 / 后续

- REQ-040 (P1, ATR 动态止损) 与 REQ-041 (P2, trailing stop) 代码已落地，但 PM 状态仍为 `pending`；建议需求/产品 Agent 在下一轮规划中显式流转至 testing 让回归覆盖。
- 现网仅 600310 一档 confirmed 触发，建议明日开盘后留意盘中 alert 是否按设计仅 14:50 后才执行实际 SELL_HALF。
