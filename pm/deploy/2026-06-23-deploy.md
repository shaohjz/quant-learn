# 部署日志：2026-06-23

**部署时间**: 2026-06-23 18:30  
**部署人**: release-agent (quant-finance-manager)  
**环境**: production (本地 Windows 服务器)  
**Git 分支**: master (commit `d260308`)

---

## 一、本次部署内容

### 1. REQ-061: sim_daily_nav.daily_return 持续为 NULL

**状态变更**: `verified` → `deployed`

**修复内容**:
- 修改 `sim/engine.py` `daily_settle()` 方法
- **根因**: `daily_return` 在 `cash_jump_detected` 时被设为 `None`，导致所有跳变日的日收益率为 NULL
- **修复**: 始终计算并存储 `daily_return`，跳变原因记录在 `cash_jump_reason` 字段中
- **修改文件**: `sim/engine.py` (第632-634行)

**验证结果**:
```
Engine init OK, account_id: 1
daily_settle result:
  daily_return: -0.9  (非 None!)
  cash_jump_detected: False
  cash_jump_reason: None
PASS: daily_return 正常计算
```

### 2. REQ-064: real_portfolio(account_id=2) 持仓价格 stale

**状态变更**: `verified` → `deployed`

**修复内容** (已在 2026-06-20 完成，本次部署到 production):
- 修改 `scripts/portfolio_alert.py`: 同时更新 account_id=1 和 account_id=2 的持仓市值
- 修改 `scripts/sync_real_position.py`: 新增 `ensure_account()` 函数，自动初始化 `sim_account` 记录
- 修复 `sim_account` 表缺少 `account_id=2` 记录的问题

---

## 二、部署步骤

### 1. Git 操作
```bash
git checkout master
git merge dev  # 已包含 REQ-061/064 修复
git add sim/engine.py
git commit -m "fix(REQ-061): sim/engine.py daily_return 不再设为 None"
git push origin master  # 失败（SSH 连接中断，不影响本地部署）
```

**注意**: `git push` 因 SSH 连接失败（10.99.19.106 port 22: Software caused connection abort），但本地 master 已包含修复，production 环境即本机，部署不受影响。

### 2. 数据库验证
```
✅ data/sim_live_mirror.db 存在
✅ sim_daily_nav 表包含 cash_jump_detected, cash_jump_reason 字段
✅ sim_account 表包含 account_id=1 和 account_id=2 的记录
```

### 3. 冒烟测试
```python
from sim.engine import SimEngine
eng = SimEngine(account_id=1)
result = eng.daily_settle(trade_date=date.today())
assert result['daily_return'] is not None  # PASS
```

**结果**: ✅ 全部通过

---

## 三、回滚方案

如部署后 30 分钟内出现问题，执行以下回滚步骤：

```bash
git revert d260308  # 回滚 engine.py 修改
# 或
git checkout HEAD~1 -- sim/engine.py  # 恢复上一版本
```

**回滚联系人**: jizhouhu  
**回滚验证**: 确认 `sim_daily_nav.daily_return` 可重新为 NULL（即恢复原逻辑）

---

## 四、状态更新

| 文件 | 状态变更 |
|------|----------|
| `pm/bugs/REQ-061-daily-return-null.md` | `verified` → `deployed` |
| `pm/bugs/REQ-064-real-portfolio-stale.md` | `verified` → `deployed` |

---

## 五、待办事项

- [ ] 修复 Git SSH 连接问题（10.99.19.106 端口 22 连接中断）
- [ ] 确认 `scripts/daily_review.py` 是否被过度简化（当前仅 323 行，缺少 `write_daily_nav` 函数）
- [ ] 监控今日 `sim_daily_nav` 数据写入是否正常（`daily_return` 不为 NULL）

---

**部署完成时间**: 2026-06-23 18:45  
**部署结果**: ✅ 成功  
**下次部署计划**: 2026-06-24 18:30
