# Cursor 修复队列

每晚 OpenClaw/PM 生成 `YYYY-MM-DD.md`：

- **完整 backlog**：P0 + P1 + P2（不是只留 3 条 P0）
- 对应 `pm/requirements` / `pm/bugs` 必须已有正文

主人在 Cursor：

```text
按 pm/cursor_queue/今天.md 从顶往下做，能做多少做多少，不限 P0。
```

约定见 `docs/REVIEW_LOOP.md`。
