# 运维巡检报告 — 2026-06-16

**巡检时间：** 2026-06-16 19:00 (Asia/Shanghai)  
**执行 Agent：** ops-agent-daily (cron: 7835f916-cb1f-485e-863a-adcd4857653e)  
**报告状态：** ⚠️ 发现问题（持续）

---

## 1. 行情源检查  ⚠️ DEGRADED

| 项目 | 状态 | 详情 |
|------|------|------|
| AKShare 实时行情 | ❌ 异常 | `Connection aborted, Remote end closed connection` (**连续第7天**) |
| 腾讯行情 API（realtime_price_gateway） | ✅ 正常 | 5只测试股票全部返回有效价格，延迟 10~17s |
| 新浪行情 API | ❌ 异常 | HTTP 403（自 2026-06-15 起） |
| 最新 CSV 数据时间 | ✅ 正常 | 所有31个文件最后日期 2026-06-15（上一交易日收盘） |
| `fetch_data.py` 备选机制 | ✅ 正常 | BaoStock 备选链路工作正常 |

**结论：** 主行情源（AKShare）持续连接失败，已连续 **7个交易日** 不可用。腾讯行情 API（realtime_price_gateway）正常，CSV 数据已更新至上一交易日收盘。

**严重程度：** S1（主源失效，备选正常）

**趋势：** AKShare 连续7天不可用，建议永久切换主源，不再等待恢复。

---

## 2. 交易 API 检查  ❌ FAIL

| 项目 | 状态 | 详情 |
|------|------|------|
| `realtime_price_gateway` 导入 | ✅ 正常 | 可正常初始化并获取行情 |
| `QmtGateway` 导入 | ⚠️ 需 event_engine | 初始化需传 `event_engine` 参数（设计如此） |
| `vnpy` 安装状态 | ❌ 未安装 | 环境中无 `vnpy` 包 |
| `xtquant` 安装状态 | ❌ 未安装 | Python 3.14 不兼容（需 3.11） |
| QMT 客户端进程 | ❌ 未运行 | `QMT.exe` / `xtquant.exe` 均未运行 |

**结论：** 交易网关依赖缺失，`vnpy`/`xtquant` 均未安装。当前 `broker.mode: sim`，不影响模拟交易，但阻碍实盘交易功能。

**严重程度：** S2（功能缺失，当前 sim 模式影响有限）

**关联：** `BUG-20260610-002` 已记录此问题（自 2026-06-10 起未修复）。

---

## 3. 数据库检查  ✅ PASS

| 数据库文件 | 可读 | 可写 | 状态 |
|-----------|------|------|------|
| `data/pm.db` | ✅ | ✅ | 读写测试通过 |
| `data/pm.db` (tasks 表) | ✅ | ✅ | 1 张表，读写正常 |

**结论：** 数据库文件正常，读写测试全部通过。

---

## 4. 磁盘 & 内存检查  ✅ PASS

| 项目 | 使用量 | 阈值 | 状态 |
|------|--------|------|------|
| 磁盘 C: | 68.2% (104.3 GB / 153.0 GB) | < 80% | ✅ 正常 |
| 磁盘 D: | 28.9% (28.9 GB / 100 GB) | < 80% | ✅ 正常 |
| 内存 | 66.5% (5.3 GB free / 16.0 GB total) | < 85% | ✅ 正常 |

**与上期对比（2026-06-15）：**
- C: 67.7% → 68.2%（+0.5pp，缓慢增长，需关注）
- 内存: 70.5% → 66.5%（改善）
- D: 28.7% → 28.9%（稳定）

**结论：** 磁盘和内存均在安全范围内。C盘使用率持续缓慢上升，建议下周关注。

---

## 5. Cron 任务检查  ✅ PASS

| 任务 | 调度 | 状态 | 下次运行 |
|------|------|------|---------|
| `ops-agent-daily` | `0 19 * * 1-5` (Asia/Shanghai +5min stagger) | ✅ 启用 | 2026-06-17 19:00 |

**任务状态：**
- `lastRunStatus: ok`
- `lastStatus: ok`
- `consecutiveErrors: 0`
- `consecutiveSkipped: 0`
- 上次运行时长：~2.4 min（2026-06-15）

**最近运行历史：**

| 运行时间 | 状态 | 时长 |
|---------|------|------|
| 2026-06-16 19:00 | 🔄 运行中 | — |
| 2026-06-15 19:00 | ✅ ok | ~2.4 min |
| 2026-06-12 19:00 | ✅ ok | ~2.9 min |
| 2026-06-11 19:00 | ✅ ok | ~3.4 min |
| 2026-06-10 19:00 | ❌ error | ~4.1 min |

**结论：** Cron 任务配置正确，连续4次运行成功（含本次），错误计数已清零。

---

## 6. 发现问题汇总

| # | 问题 | 严重等级 | 状态 | 首次发现 | 持续天数 |
|---|------|---------|------|---------|---------|
| 1 | AKShare 行情源连接失败，主源持续不可用 | S1 | ⚠️ 备选可用 | 2026-06-09 | **第7天** |
| 2 | `vnpy`/`xtquant` 未安装，交易网关无法加载 | S2 | 待修复 | 2026-06-10 | 第7天 |
| 3 | 新浪行情 API HTTP 403 | S3 | 低优先级 | 2026-06-15 | 第2天 |

> **问题 1：** AKShare 已连续7天不可用，远超临时性故障范围。建议永久切换备选源。
> **问题 2：** Python 3.14 与 xtquant 不兼容是根本原因。如需实盘，需 Python 3.11 独立环境。
> **问题 3：** 新浪 API 返回 403，低优先级（腾讯 API 可用）。

---

## 7. 自动恢复尝试

### 尝试 1：AKShare 恢复（S1）

```python
import akshare as ak
df = ak.stock_zh_a_spot_em()
```

**结果：** ❌ 同样 `Connection aborted` — AKShare 远端问题，非本机网络问题。  
**备选方案：** `realtime_price_gateway.py` 通过腾讯 API 正常提供服务，延迟 10~17s 可接受。

**建议：** AKShare 连续7天不可用，建议：
1. **立即永久切换**：修改 `realtime_price_gateway.py`，将 AKShare 从默认 source 中移除
2. 检查 AKShare GitHub Issues 是否有相关公告（可能被反爬封禁）
3. 考虑增加 Tushare 作为备选（需 token）

### 尝试 2：vnpy/xtquant 安装（S2）

**结果：** ❌ 不适用 — 根本问题是 Python 3.14 与 xtquant 编译版本不兼容。  
**真正解法：** 如需启用 QMT 实盘，安装 Python 3.11 并创建独立 venv。

---

## 8. Bug 跟踪状态

| Bug ID | 标题 | 等级 | 状态 | 创建日期 |
|--------|------|------|------|---------|
| `BUG-20260609-001` | AKShare 行情源失效 | S1 | ❌ 未修复 | 2026-06-09 |
| `BUG-20260610-001` | AKShare 持续失效 | S1 | ❌ 未修复 | 2026-06-10 |
| `BUG-20260610-002` | xtquant/vnpy 导入失败 | S2 | ❌ 未修复 | 2026-06-10 |
| `BUG-20260611-001` | AKShare 持续失效（更新） | S1 | ❌ 未修复 | 2026-06-11 |
| `BUG-20260611-002` | vnpy 未安装 | S2 | ❌ 未修复 | 2026-06-11 |
| `BUG-20260611-003` | Cron delivery 配置 | S3 | ✅ 已修复 | 2026-06-11 |
| `BUG-20260612-003` | AKShare 持续失效（跟踪） | S1 | ❌ 未修复 | 2026-06-12 |
| `BUG-2026-06-15-data-update-interrupted` | 数据更新中断 | P0 | 🔄 进行中 | 2026-06-15 |

**行动：** AKShare 问题已连续7天，建议合并所有相关 Bug 为一个主 Bug，状态标记为 `WONT_FIX`（远端问题，无法修复），并在代码中永久切换备选源。

---

## 9. 建议行动

### 立即（S1）
1. **永久切换主行情源**：修改 `gateways/realtime_price_gateway.py`，将 AKShare 从数据源列表中移除或降至最低优先级
2. AKShare 连续7天不可用，关闭相关调用，避免每次运行都浪费时间等待超时（每次约15秒 × 重试次数）

### 短期（S2）
3. **Python 3.11 环境**：如需启用 QMT 实盘，安装 Python 3.11 并创建独立 venv
4. 如不需要 QMT，在配置中明确禁用，避免导入失败报错影响日志清洁度

### 中期（S3）
5. 新浪行情 API 返回 403，如有使用需更新请求头或切换接口
6. C盘使用率 68.2%，持续缓慢上升，建议下周清理或扩容

---

## 10. 趋势对比（近7天）

| 日期 | AKShare | 数据库 | 磁盘 C: | 内存 | Cron | 新问题 |
|------|---------|--------|---------|------|------|--------|
| 06-09 | ❌ | ✅ | 64.6% | 65.5% | ✅ | AKShare 首次失效 |
| 06-10 | ❌ | ✅ | 69.5% | 65.0% | ❌ | xtquant 导入失败 |
| 06-11 | ❌ | ✅ | 68.1% | 62.3% | ✅ | — |
| 06-12 | ❌ | ✅ | 68.1% | 60.9% | ✅ | — |
| 06-13 | — | — | — | — | — | 周末 |
| 06-14 | — | — | — | — | — | 周末 |
| 06-15 | ❌ | ✅ | 67.7% | 70.5% | ✅ | 新浪 API 403 |
| 06-16 | ❌ | ✅ | 68.2% | 66.5% | ✅ | — |

**正向趋势：** 数据库、Cron 均稳定正常，内存有所改善。  
**负向趋势：** AKShare 主源已连续7天不可用；C盘使用率缓慢上升需关注。

---

**巡检完成时间：** 2026-06-16 19:08  
**执行结果：** OPS_DONE（有异常，详见上文）

> ⚠️ **建议**：AKShare 已连续7天不可用，建议本周内完成主行情源永久切换，避免长期依赖失效方案。
