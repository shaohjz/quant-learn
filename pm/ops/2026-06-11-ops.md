# 运维巡检报告 — 2026-06-11

**巡检时间：** 2026-06-11 19:00 (Asia/Shanghai)  
**执行 Agent：** ops-agent-daily (cron: 7835f916-cb1f-485e-863a-adcd4857653e)  
**报告状态：** ⚠️ 发现问题

---

## 1. 行情源检查  ⚠️ DEGRADED

| 项目 | 状态 | 详情 |
|------|------|------|
| AKShare 实时行情 | ❌ 异常 | `Connection aborted, Remote end closed connection without response` |
| BaoStock 备选行情 | ✅ 正常 | `fetch_data.py` 自动切换成功，2/2 股票拉取成功 |
| 最新 CSV 数据时间 | ⚠️ 较旧 | 最新 `data/*.csv` 更新于 18:37（约 23 分钟前，超过 5 分钟阈值） |
| `fetch_data.py` 自动恢复 | ✅ 成功 | 本次巡检触发后，BaoStock 备选方案自动接管，数据拉取成功 |

**结论：** 主行情源（AKShare）持续连接失败（连续第3天），但 `fetch_data.py` 的 BaoStock 备选机制工作正常，数据可正常更新。

**严重程度：** S1（主源失效，备选可用）

**趋势：** 与 2026-06-09 报告相同问题，AKShare 仍未恢复。需考虑永久切换或增加更多备选源。

---

## 2. 交易 API 检查  ❌ FAIL

| 项目 | 状态 | 详情 |
|------|------|------|
| `realtime_price_gateway` 导入 | ❌ 失败 | `No module named 'vnpy'` |
| `qmt_gateway` 导入 | ❌ 失败 | `No module named 'vnpy'` |
| `xtquant` 导入 | ❌ 失败 | `No module named 'xtshare'` |
| vnpy 安装状态 | ❌ 未安装 | `.venv` 中无 `vnpy` 包 |

**结论：** 交易网关（QMT/vnpy）依赖缺失，`vnpy` 未安装在虚拟环境中。若需启用实盘交易模拟，需安装 `vnpy` 及 `vnpy-qmt`。

**严重程度：** S2（功能缺失，当前为 sim 模式影响有限）

---

## 3. 数据库检查  ✅ PASS

| 数据库文件 | 可读 | 可写 | 路径 |
|-----------|------|------|------|
| `data/pm.db` | ✅ | ✅ | data/ |
| `data/sim.db` | ✅ | ✅ | data/ |
| `data/sim_live_mirror.db` | ✅ | ✅ | data/ |
| `pm.db`（根目录） | ✅ | ✅ | 项目根目录 |

**结论：** 所有数据库文件均可正常读写，无损坏。写入测试通过（创建/删除测试表成功）。

---

## 4. 磁盘 & 内存检查  ✅ PASS

| 项目 | 使用量 | 阈值 | 状态 |
|------|--------|------|------|
| 磁盘 C: | 64.6% (98.7 GB / 152.6 GB) | < 80% | ✅ 正常 |
| 磁盘 D: | 36.7% (36.7 GB / 100 GB) | < 80% | ✅ 正常 |
| 内存 | 62.3% (9.8 GB / 15.7 GB) | < 85% | ✅ 正常 |
| 空闲 C: 空间 | 52.7 GB | — | ✅ 充足 |

**结论：** 磁盘和内存均在安全范围内。

---

## 5. Cron 任务检查  ⚠️ DEGRADED

| 任务 | 调度 | 状态 | 上次运行 | 下次运行 |
|------|------|------|---------|---------|
| `ops-agent-daily` | `0 19 * * 1-5` (Asia/Shanghai) | ✅ 启用 | 2026-06-10（error） | 2026-06-11 19:00（本次） |

**诊断信息（上次运行）：**
- `lastRunStatus`: `error`
- `consecutiveErrors`: 1
- 错误原因：`⚠️ Message failed`（delivery.mode=none，消息发送失败，非任务逻辑错误）

**结论：** Cron 任务配置正确，定时触发正常。上次运行报错是因为 `delivery.mode=none` 导致消息投递失败，实际巡检逻辑应已执行。建议将 delivery.mode 改为 `announce` 以便接收巡检结果。

**严重程度：** S3（配置问题，不影响执行）

---

## 6. 发现问题汇总

| # | 问题 | 严重等级 | 状态 | 首次发现 |
|---|------|---------|------|---------|
| 1 | AKShare 行情源连接失败，主源持续不可用 | S1 | ⚠️ 备选可用 | 2026-06-09 |
| 2 | `vnpy` 未安装，交易网关（QMT）无法加载 | S2 | 待修复 | 2026-06-11 |
| 3 | Cron `delivery.mode=none` 导致消息投递失败 | S3 | 配置问题 | 2026-06-10 |

---

## 7. 自动恢复尝试

### 尝试 1：AKShare 恢复（S1）

```python
import akshare as ak
df = ak.stock_zh_a_spot_em()
```

**结果：** ❌ 同样 `Connection aborted` — AKShare 远端问题，非本机网络问题。  
**备选方案：** `fetch_data.py` 已内置 BaoStock 自动切换，本次运行成功（2/2）。

### 尝试 2：安装 vnpy（S2）

```bash
.venv/Scripts/pip.exe install vnpy vnpy-qmt
```

**结果：** ⏳ 待执行 — 需确认是否需要 QMT 实盘功能（当前 `config.yaml` 中 `broker.mode: sim`）。

---

## 8. 建议行动

### 立即（S1）
1. **AKShare 长期失效**：考虑永久切换至 BaoStock / Tushare 作为主源，或增加多源负载均衡
2. 检查 AKShare GitHub Issues 是否有类似报告（可能是版本问题）

### 短期（S2）
3. **安装 vnpy**：如需要 QMT 交易功能，执行：
   ```bash
   cd C:\Users\Administrator\.openclaw\workspace\quant-learn
   .venv\Scripts\pip.exe install vnpy vnpy-qmt
   ```
4. 如不需要 QMT，在 `config.yaml` 中明确禁用，避免导入失败报错

### 配置优化（S3）
5. **Cron 告警**：将 `ops-agent-daily` 的 `delivery.mode` 改为 `announce`，以便巡检结果推送到企微

---

**巡检完成时间：** 2026-06-11 19:06  
**执行结果：** OPS_DONE（有异常，详见上文）

> 连续3天 AKShare 不可用，建议升级为 Bug 跟踪。
