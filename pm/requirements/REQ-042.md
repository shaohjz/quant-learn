"""
REQ-042: 量化通知/盘中盯盘 去大模型化 — 纯代码 + 企微 Webhook 直推
======================================================================
提出人: jizhouhu
提出时间: 2026-05-27 17:19
优先级: P1
状态: testing

## 动机
当前所有量化通知（盘前/开盘/收盘/盘中盯盘）都依赖 cron → agentTurn → 大模型推理 → 发消息。
每次任务都消耗大量 token，而且有思考延迟。

## 目标
纯代码脚本（Python）直接跑逻辑 → 结果 JSON → 调用企微机器人 Webhook → 推送到群/个人。

不依赖任何大模型推理。

## 方案概要

### 1. 企微自定义机器人 Webhook
- 在企微群里添加一个「自定义机器人」，拿到 Webhook URL
- 脚本用 requests.post(webhook_url, json={msgtype, markdown/text, ...}) 推送
- 支持 markdown 格式（标题、加粗、代码块）

### 2. 改造对象
| 现有 cron | 改造后 |
|---|---|
| auction-9:24 集合竞价快报 | Python 脚本直接跑 → POST Webhook |
| morning-brief-9:25 多策略简报 | 同上 |
| auction-9:31 开盘快报 | 同上 |
| post-15:05 收盘复盘 | 同上 |
| daily-review 15:10 | 同上 |
| intraday-watch（盘中盯盘） | 同上 |

### 3. 执行方式
cron 从 agentTurn 改为 systemEvent + 本地脚本：
option A: cron → exec python script.py → script 内 POST webhook
option B: cron → 直接 curl/webhook（更简单）

### 4. 消息格式
企微机器人支持 markdown：
```json
{
  "msgtype": "markdown",
  "markdown": {
    "content": "## 🔔 阈值触发\n**中国电信** 跌破 ¥6.20"
  }
}
```

## 收益
- 💰 token 消耗降为 0（这些任务不再调大模型）
- ⚡ 延迟更低（省去大模型推理时间）
- 🔧 更可控（纯代码逻辑，不会 LLM 幻觉）

## 依赖
- [ ] 企微自定义机器人 Webhook URL（需要用户在企微群里手动添加）
- [ ] 脚本里硬编码或读环境变量的 WEBHOOK_URL

## 已完成

### 新增脚本
| 脚本 | 功能 | 对应 cron |
| --- | --- | --- |
| `scripts/wecom_webhook.py` | 企微 Webhook 推送公共模块 | — |
| `scripts/notify_auction.py` | 集合竞价快报（9:24） | auction-9:24 |
| `scripts/notify_morning_brief.py` | 多策略简报（9:25） | morning-brief-9:25 |
| `scripts/notify_open.py` | 开盘快报（9:31） | auction-9:31 |
| `scripts/notify_review.py` | 收盘复盘（15:05）+ 日终总结（15:10） | post-15:05, daily-review 15:10 |
| `scripts/notify_intraday.py` | 盘中盯盘（盘中每 30 分） | intraday-watch |

### 执行方式
所有脚本通过 `python scripts/notify_xxx.py` 直接运行，不依赖任何大模型推理。

### Webhook URL 读取优先级
1. 环境变量 `WECOM_WEBHOOK_URL`
2. `config.local.yaml` → `notifier.wecom_webhook`
3. `config.yaml` → `notifier.wecom_webhook`

### 自测结果
- ✅ `wecom_webhook.py` — Webhook 推送成功
- ✅ `notify_auction.py` — 集合竞价快报生成正常
- ✅ `notify_morning_brief.py` — 多策略简报生成正常
- ✅ `notify_open.py` — 开盘快报生成正常
- ✅ `notify_review.py --mode review` — 收盘复盘正常
- ✅ `notify_review.py --mode summary` — 日终总结正常
- ✅ `notify_intraday.py` — 盘中盯盘正常

### 下一步
1. 更新 cron 从 agentTurn → systemEvent（调用 `python scripts/notify_xxx.py`）
2. 用户确认企微 Webhook URL 已正确配置
